CORELABTECH — MASTER PRODUCTION TUTORIAL 2026
============================================================================
WERSJA: FINAL CURATED + FULL RAW ARCHIVE
DATA GENERACJI: 2026-05-30 18:18:11

ŹRÓDŁA:
1. Wklejony tekst(68).txt
2. Wklejony tekst (2)(3).txt
3. Wklejony tekst (3).txt

ZASADA DOKUMENTU:
- Część A to uporządkowany, architektonicznie spójny tutorial produkcyjny.
- Część B rozdziela DEV / QA / PROD.
- Część C zawiera checklisty.
- Część D zawiera troubleshooting.
- Część E zawiera pełną treść 1:1 wszystkich trzech plików źródłowych.
- Dzięki temu finalna instrukcja jest spójna, ale żadna treść źródłowa nie została usunięta.

============================================================================
SPIS TREŚCI
============================================================================
CZĘŚĆ A — FINALNY TUTORIAL PRODUKCYJNY
CZĘŚĆ B — WARIANTY ŚRODOWISK: DEV / QA / PROD
CZĘŚĆ C — CHECKLISTY OPERACYJNE
CZĘŚĆ D — TROUBLESHOOTING
CZĘŚĆ E — PEŁNE ARCHIWUM ŹRÓDŁOWE 1:1

============================================================================
CZĘŚĆ A — FINALNY TUTORIAL PRODUKCYJNY
============================================================================
Ten rozdział jest właściwą instrukcją produkcyjną.
Jeżeli sekcja archiwalna na końcu zawiera starszy wariant, pierwszeństwo ma część A/B.

============================================================================
0. CEL DOKUMENTU
============================================================================
Ten dokument scala trzy źródłowe tutoriale CoreLabTech w jedną wersję produkcyjną.
Łączy Docker, Flask, PostgreSQL, Auth, RBAC, Security, Playwright, Gatling, Monitoring, Nginx, HTTPS, backup i troubleshooting.
Dokument jest przygotowany tak, aby można go było wykonać krok po kroku na VPS.

============================================================================
1. AKTUALNY STATUS PROJEKTU
============================================================================
Zakładany status przed wdrożeniem:
- Docker działa lokalnie
- PostgreSQL działa
- Flask działa
- Docker Compose działa
- Gunicorn działa
- login działa
- role działają
- /api/health działa
- CSV/FIT upload działa
- AI analysis działa
- /api/sessions działa
- /api/user_trends działa
- Playwright działa po poprawkach auth/network
- Gatling działa po poprawkach redirect/performance endpoints

Konta lokalne/demo:
admin@corelabtech.local / Admin123!
operator@corelabtech.local / Operator123!
researcher@corelabtech.local / Researcher123!
viewer@corelabtech.local / Viewer123!

Na produkcji demo hasła muszą zostać zmienione albo konta demo muszą zostać usunięte.

============================================================================
2. FINALNA ARCHITEKTURA PRODUKCYJNA
============================================================================
Internet
  ↓
Domena home.pl
  ↓
Nginx HTTPS
  ↓
Docker Compose
  ↓
Gunicorn Flask
  ↓
PostgreSQL
  ↓
Monitoring / Logs / Reports

CoreLabTech działa jako production-grade AI telemetry & QA research platform.

============================================================================
3. FINALNY MODEL DOSTĘPU
============================================================================
Publiczne bez logowania:
/
/about
/technology
/contact
/qa
/performance-tests
/ai-testing-lab
/login
/api/health

Po logowaniu:
/chamber
/research
/ai-lab
/profile
/logout
/api/sessions

Operator + Admin:
/chamber
/upload_csv
/upload_fit
/api/save_phase
/api/save_full_session
/api/during_merge

Researcher + Admin:
/ai-qa-lab
/api/run_analysis
/api/user_trends/<user_id>
/api/ai_qa/*
/report

Admin only:
/admin
/debug/db
/api/run_playwright
/api/qa/run_pipeline
/api/performance/run
/api/performance/history
/api/performance/latest
/api/users
/api/delete_user
/api/delete_sessions
/admin/playwright-report/*
/admin/gatling-report/*

============================================================================
4. ROLE RBAC
============================================================================
viewer:
- widzi tylko własne sesje
- nie tworzy danych
- nie uruchamia AI

operator:
- tworzy sesje HBOT
- wrzuca CSV/FIT
- zapisuje PRE/DURING/POST
- uruchamia merge timeline

researcher:
- uruchamia AI analysis
- analizuje trendy
- widzi AI QA Lab
- może analizować dane badawcze

admin:
- pełna kontrola
- users
- DB debug
- Playwright QA
- Gatling performance
- monitoring
- reports

============================================================================
5. FINALNA STRUKTURA PROJEKTU
============================================================================
Rekomendowana struktura:
corelabtech_final_clean/
├── app.py
├── database_postgres.py
├── init_postgres_db.py
├── seed_postgres_db.py
├── .env
├── .env.example
├── requirements.txt
├── .gitignore
├── .dockerignore
├── Dockerfile
├── docker-compose.yml
├── entrypoint.sh
├── auth/
├── security/
├── routes/
├── services/
├── templates/
├── logs/
├── data/uploads/temp/
├── nginx/
├── monitoring/
└── tests/

Decyzja architektoniczna:
health_routes.py trzymaj w routes/health_routes.py.
Folder monitoring/ zostaje dla Prometheus, Grafana, dashboardów i alertów.
Starszy wariant monitoring/health_routes.py traktuj jako legacy/debug.

============================================================================
6. POPRAWNA KOLEJNOŚĆ WDROŻENIA
============================================================================
1. Przygotuj repozytorium.
2. Uporządkuj .env.
3. Upewnij się, że .env nie jest w repo.
4. Przygotuj Dockerfile.
5. Przygotuj docker-compose.yml dla produkcji.
6. Sprawdź Auth + RBAC.
7. Sprawdź CSRF i security.
8. Sprawdź upload validation.
9. Sprawdź Playwright lokalnie/QA.
10. Sprawdź Gatling lokalnie/QA.
11. Przygotuj VPS.
12. Ustaw DNS.
13. Zainstaluj Docker i Git na VPS.
14. Sklonuj repo.
15. Utwórz produkcyjny .env.
16. Zbuduj kontenery.
17. Uruchom PostgreSQL i web.
18. Wykonaj init DB.
19. Wykonaj seed DB.
20. Uruchom Nginx.
21. Włącz HTTPS.
22. Ustaw firewall.
23. Przetestuj login, role i health.
24. Włącz backup.
25. Włącz logrotate.
26. Zweryfikuj monitoring.
27. Zmień/usuń konta demo.
28. Zrób finalny test produkcyjny.

============================================================================
7. VPS, DOMENA, DNS
============================================================================
Zakładany hosting: VPS Linux M w home.pl.
Zakładana domena: corelabtech.pl.
Aplikacja: app.corelabtech.pl.

DNS:
Typ: A
Host: app
Wartość: IP_VPS

Test:
ping app.corelabtech.pl
nslookup app.corelabtech.pl

============================================================================
8. SSH, SYSTEM, DOCKER, GIT
============================================================================
ssh root@IP_SERWERA
apt update && apt upgrade -y
apt install docker.io docker-compose-plugin -y
systemctl enable docker
systemctl start docker
docker info
apt install git -y
cd /opt
git clone https://github.com/TWOJE_REPO/corelabtech.git
cd corelabtech
Repo powinno być prywatne.

============================================================================
9. .ENV PRODUKCYJNY
============================================================================
Plik: /opt/corelabtech/.env

SECRET_KEY=SUPER_RANDOM_SECRET_KEY
DATABASE_URL=postgresql://corelabtech_user:SUPER_DB_PASSWORD@db:5432/corelabtech
APP_ENV=production
FLASK_ENV=production
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SECURE=True
REMEMBER_COOKIE_HTTPONLY=True
PERFORMANCE_TESTING=false

Generowanie SECRET_KEY:
python3 -c "import secrets; print(secrets.token_hex(32))"

Nigdy nie używaj produkcyjnie:
SECRET_KEY=test
SECRET_KEY=dev-secret-change-me
SECRET_KEY=corelabtech-super-secret-key-change-this

.gitignore musi zawierać:
.env

============================================================================
10. REQUIREMENTS.TXT
============================================================================
Flask
flask-cors
flask-login
flask-wtf
flask-limiter
email-validator
python-dotenv
gunicorn
psycopg2-binary
fitparse
pandas
numpy
prometheus-flask-exporter

============================================================================
11. DOCKERFILE PRODUKCYJNY — FINALNY KIERUNEK
============================================================================
Używaj python:3.11-bookworm.
Nie używaj slim, jeżeli Playwright/Chromium/Maven/Java sprawiają problemy.
Dockerfile ma instalować zależności systemowe, Python requirements, Playwright Chromium, katalogi logs/data/test-results.
Pełne warianty Dockerfile znajdują się w archiwum źródłowym na końcu pliku.

============================================================================
12. DOCKER-COMPOSE PRODUKCYJNY — FINALNE ZASADY
============================================================================
web:
- build: .
- container_name: corelabtech_web
- restart: unless-stopped
- ports: 127.0.0.1:5000:5000
- env_file: .env
- PERFORMANCE_TESTING=false
- depends_on db healthy
- volumes: logs, data, playwright-report, test-results, gatling target
- command: gunicorn app:app --workers 3 --bind 0.0.0.0:5000 --timeout 300

db:
- image: postgres:16
- container_name: corelabtech_postgres
- restart: unless-stopped
- POSTGRES_DB=corelabtech
- POSTGRES_USER=corelabtech_user
- POSTGRES_PASSWORD=SUPER_DB_PASSWORD
- volume postgres_data
- healthcheck pg_isready

Na produkcji NIE wystawiaj 5432.
Na produkcji NIE wystawiaj 5000 publicznie.
Publicznie ma działać tylko Nginx na 80/443.

============================================================================
13. BUILD, START, INIT, SEED
============================================================================
docker compose build --no-cache
docker compose up -d
docker ps
docker compose exec web python init_postgres_db.py
docker compose exec web python seed_postgres_db.py
curl http://127.0.0.1:5000/api/health

============================================================================
14. POSTGRESQL USERS
============================================================================
Tabela users powinna mieć minimum:
id
user_id
email
password_hash
role
is_active

Sprawdzenie:
SELECT column_name, data_type FROM information_schema.columns WHERE table_name='users' ORDER BY ordinal_position;

Jeżeli brakuje pól, dodaj ALTER TABLE z archiwum źródłowego.
Jeżeli has_password=false, popraw seed_postgres_db.py na ON CONFLICT DO UPDATE.

============================================================================
15. AUTH + RBAC
============================================================================
Folder auth zawiera:
auth/__init__.py
auth/user_model.py
auth/decorators.py
auth/auth_routes.py

role_required sprawdza current_user.is_authenticated oraz current_user.role.
Login używa flask-login, check_password_hash i limiter 5 per minute.
Nie dawaj csrf.exempt na /login.

============================================================================
16. APP.PY — FINALNA KOLEJNOŚĆ
============================================================================
1. app = Flask(__name__)
2. app.secret_key z SECRET_KEY
3. SESSION_COOKIE_HTTPONLY=True
4. REMEMBER_COOKIE_HTTPONLY=True
5. SESSION_COOKIE_SECURE z .env
6. configure_security_headers(app)
7. csrf.init_app(app)
8. limiter.init_app(app)
9. LoginManager init
10. user_loader
11. register_blueprint(auth_bp)
12. register_blueprint(health_bp)
13. errorhandler 401/403

============================================================================
17. SECURITY HARDENING
============================================================================
Folder security:
security/__init__.py
security/upload_validation.py
security/limiter.py
security/headers.py
security/csrf.py

Headers:
X-Content-Type-Options
X-Frame-Options
X-XSS-Protection
Referrer-Policy
Content-Security-Policy

Limiter:
default 500 per day, 100 per hour
enabled=not PERFORMANCE_TESTING

Produkcja: PERFORMANCE_TESTING=false
QA/Gatling: PERFORMANCE_TESTING=true

============================================================================
18. CSRF
============================================================================
Login powinien być chroniony przez CSRF.
login.html musi mieć hidden csrf_token.
Błąd bez tokena: Bad Request — The CSRF token is missing.
API upload może mieć csrf.exempt, ale wtedy musi mieć login_required, role_required, limiter i walidację uploadu.

============================================================================
19. UPLOAD CSV/FIT
============================================================================
Endpointy upload_csv i upload_fit:
- csrf.exempt
- route POST
- login_required
- role_required operator/admin
- limiter 20 per hour
- validate_extension
- validate_file_size
- safe_upload_filename
- zapis do data/uploads/temp

============================================================================
20. API SESSIONS I USER TRENDS
============================================================================
/api/sessions:
admin i researcher widzą wszystkie sesje.
operator i viewer widzą tylko własne sesje.

/api/user_trends/<user_id>:
admin i researcher mogą czytać użytkowników badawczo.
viewer/operator mogą czytać tylko własne dane.

============================================================================
21. AI TESTING LAB
============================================================================
/ai-testing-lab — publiczny opis.
/ai-qa-lab — panel roboczy tylko researcher/admin.
/api/qa/run_pipeline — admin only.
Endpointy akcyjne AI QA zabezpiecz login_required, role_required i w razie potrzeby limiter.

============================================================================
22. PERFORMANCE TESTS
============================================================================
/performance-tests publicznie pokazuje opis architektury.
Przyciski Run Performance widzi tylko admin.
API performance run/latest/history jest admin only.
Gatling uruchamiaj w QA/local z PERFORMANCE_TESTING=true.

============================================================================
23. PLAYWRIGHT
============================================================================
Używaj playwright test.
Nie używaj playwright test --reporter=list, jeżeli chcesz HTML report.
Raport HTML: playwright-report/
Konfiguracja: workers 1, retries 0, fullyParallel false, html reporter.

============================================================================
24. GATLING
============================================================================
Gatling testuje sessions API, merge engine, AI analysis, upload CSV/FIT, auth i performance endpoints.
Raporty HTML: tests/performance/gatling/target/
Typowe problemy:
- 429 Too Many Requests
- redirect do login zamiast JSON
- jsonPath failed to parse HTML
- brak auth/session

============================================================================
25. NGINX
============================================================================
apt install nginx -y
Plik: /etc/nginx/sites-available/corelabtech
Proxy ma kierować na http://127.0.0.1:5000.
Ustaw nagłówki Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto.
client_max_body_size 100M.
Timeouty 300.
Aktywacja: ln -s do sites-enabled, nginx -t, systemctl reload nginx.

============================================================================
26. HTTPS
============================================================================
apt install certbot python3-certbot-nginx -y
certbot --nginx -d app.corelabtech.pl
Test:
https://app.corelabtech.pl
https://app.corelabtech.pl/login
https://app.corelabtech.pl/api/health
Dopiero po HTTPS ustaw SESSION_COOKIE_SECURE=True i zrestartuj web.

============================================================================
27. FIREWALL
============================================================================
apt install ufw -y
ufw allow OpenSSH
ufw allow 80
ufw allow 443
ufw enable
ufw status
Publicznie tylko 80 i 443.
Nie publicznie: 5000, 5432.

============================================================================
28. MONITORING
============================================================================
Prometheus:
monitoring/prometheus.yml
scrape_interval 15s
target web:5000

Flask:
prometheus-flask-exporter
PrometheusMetrics(app)
/metrics

Grafana dashboardy:
CPU, RAM, Docker, PostgreSQL, API latency, upload rate, AI usage, HTTP 500.

============================================================================
29. BACKUP I LOGROTATE
============================================================================
Backup manualny:
docker compose exec -T db pg_dump -U corelabtech_user corelabtech > backups/corelabtech_$(date +%Y%m%d_%H%M%S).sql

Cron:
0 2 * * * cd /opt/corelabtech && docker compose exec -T db pg_dump -U corelabtech_user corelabtech > backups/corelabtech_$(date +\%Y\%m\%d_\%H\%M\%S).sql

Logrotate:
/opt/corelabtech/logs/*.log daily rotate 14 compress missingok notifempty copytruncate

============================================================================
30. AKTUALIZACJA APLIKACJI
============================================================================
cd /opt/corelabtech
git pull
docker compose build --no-cache
docker compose up -d

NIGDY nie rób na produkcji:
docker compose down -v
bo usuwa dane PostgreSQL z volume.

============================================================================
31. FINALNA CHECKLISTA BEZPIECZEŃSTWA
============================================================================
✅ SECRET_KEY losowy
✅ demo passwords zmienione
✅ HTTPS działa
✅ SESSION_COOKIE_SECURE=True
✅ PostgreSQL niepubliczny
✅ tylko 80/443 publiczne
✅ firewall działa
✅ /api/health działa
✅ login działa po HTTPS
✅ role działają
✅ backup działa
✅ logrotate działa
✅ .env nie jest w repo
✅ run performance ukryty dla non-admin
✅ /debug/db tylko admin
✅ Playwright report tylko admin
✅ Gatling report tylko admin
✅ PERFORMANCE_TESTING=false na produkcji
✅ monitoring działa
✅ brak prawdziwych danych medycznych w demo

============================================================================
CZĘŚĆ B — WARIANTY ŚRODOWISK
============================================================================
Ta część rozdziela informacje, które w plikach źródłowych były wymieszane.

============================================================================
B1. LOCAL DEV
============================================================================
Cel: szybki development.
SESSION_COOKIE_SECURE=False.
PERFORMANCE_TESTING=true opcjonalnie.
Można mapować port 5000 publicznie lokalnie.
Można mapować PostgreSQL 5432 lokalnie do pgAdmin.

============================================================================
B2. LOCAL QA
============================================================================
Cel: Playwright, Gatling, raporty HTML.
PERFORMANCE_TESTING=true.
Limiter wyłączony.
Gunicorn może mieć workers 1 dla stabilności testów.

============================================================================
B3. PRODUCTION VPS
============================================================================
Cel: publiczna aplikacja za Nginx HTTPS.
SESSION_COOKIE_SECURE=True.
PERFORMANCE_TESTING=false.
PostgreSQL bez publicznego 5432.
Flask/Gunicorn tylko na 127.0.0.1:5000.
Publicznie tylko 80/443.

============================================================================
B4. LEGACY / DEBUG
============================================================================
Do legacy/debug zalicz:
- monitoring/health_routes.py zamiast routes/health_routes.py
- publiczne wystawianie PostgreSQL na produkcji
- SESSION_COOKIE_SECURE=False na produkcji
- PERFORMANCE_TESTING=true na produkcji
- docker compose down -v
- Playwright z --reporter=list, gdy potrzebny jest HTML
- Gatling bez auth/session
- brak csrf_token w login.html

============================================================================
CZĘŚĆ C — CHECKLISTY OPERACYJNE
============================================================================
Before deploy, after deploy, security, monitoring i QA.

============================================================================
C1. BEFORE DEPLOY
============================================================================
[ ] Repo prywatne
[ ] .env w .gitignore
[ ] SECRET_KEY wygenerowany
[ ] demo passwords zmienione
[ ] docker-compose produkcyjny gotowy
[ ] PostgreSQL bez publicznego portu
[ ] port 5000 tylko 127.0.0.1
[ ] requirements.txt aktualny
[ ] Dockerfile buduje się bez błędów
[ ] /api/health działa lokalnie
[ ] login działa lokalnie
[ ] role działają lokalnie
[ ] Playwright przechodzi
[ ] Gatling przechodzi w QA

============================================================================
C2. AFTER DEPLOY
============================================================================
[ ] docker ps pokazuje healthy
[ ] /api/health działa lokalnie
[ ] domena wskazuje VPS
[ ] Nginx działa
[ ] HTTPS działa
[ ] /login działa po HTTPS
[ ] SESSION_COOKIE_SECURE=True
[ ] firewall działa
[ ] tylko 80/443 publiczne
[ ] PostgreSQL niewidoczny publicznie
[ ] admin login działa
[ ] viewer nie widzi admin tools
[ ] researcher widzi AI QA
[ ] admin widzi performance actions
[ ] backup działa
[ ] logrotate działa
[ ] monitoring działa

============================================================================
CZĘŚĆ D — TROUBLESHOOTING
============================================================================
Najważniejsze problemy zebrane z dokumentów źródłowych.

============================================================================
D1. DOCKER
============================================================================
Kontener nie wstaje: docker compose logs web --tail=200.
Szukaj: Worker failed to boot.
Stary obraz: docker compose down && docker compose build --no-cache && docker compose up -d.
Git Bash może zamieniać /app na Windows path — używaj sh -lc.
Volume busy — usuń zawartość katalogu, nie sam katalog.

============================================================================
D2. AUTH / CSRF
============================================================================
Błąd: Bad Request — The CSRF token is missing.
Powód: brak csrf_token w login.html.
Nie wyłączaj CSRF na loginie.

============================================================================
D3. PLAYWRIGHT
============================================================================
Brak HTML report: nie używaj --reporter=list.
Użyj: playwright test.
Sprawdź baseURL i czy aplikacja działa pod 127.0.0.1:5000.

============================================================================
D4. GATLING
============================================================================
Błąd jsonPath failed to parse HTML oznacza zwykle HTML zamiast JSON.
Powody: redirect do login, brak auth, limiter 429, zły endpoint.
Rozwiązanie: auth/session w teście, PERFORMANCE_TESTING=true w QA, popraw endpointy.

============================================================================
D5. NGINX / HTTPS
============================================================================
nginx -t
systemctl reload nginx
curl -I http://app.corelabtech.pl
curl -I https://app.corelabtech.pl
Sprawdź X-Forwarded-Proto i SESSION_COOKIE_SECURE.

============================================================================
CZĘŚĆ E — PEŁNE ARCHIWUM ŹRÓDŁOWE 1:1
============================================================================
Poniżej znajduje się pełna treść trzech plików źródłowych bez usuwania treści.
Ta część jest archiwum wiedzy i historii zmian.
Warianty sprzeczne z częścią A/B traktuj jako starsze warianty, notatki debugowe albo kontekst historyczny.

============================================================================
ARCHIWUM ŹRÓDŁOWE — PLIK 1: Wklejony tekst(68).txt
============================================================================

CORELABTECH — FINALNY TUTORIAL WDROŻENIA PRODUKCYJNEGO
VPS Linux M (home.pl)
________________________________________
1. Aktualny status projektu
Masz już:
✅ Docker działa lokalnie
✅ PostgreSQL działa
✅ Flask działa
✅ login działa
✅ role działają
✅ /api/health działa
✅ Docker Compose działa
Konta lokalne:
admin@corelabtech.local / Admin123!
operator@corelabtech.local / Operator123!
researcher@corelabtech.local / Researcher123!
viewer@corelabtech.local / Viewer123!
To oznacza, że:
•	aplikacja jest gotowa do deployu, 
•	teraz przechodzisz do produkcji. 
________________________________________
2. Finalna architektura produkcyjna
Docelowo:
Internet
    ↓
Domena home.pl
    ↓
Nginx HTTPS
    ↓
Docker Compose
    ↓
Gunicorn Flask
    ↓
PostgreSQL
________________________________________
3. Co będzie potrzebne
Musisz mieć:
✅ VPS Linux M w home.pl
✅ domenę
✅ SSH access
✅ Docker lokalnie działający
________________________________________
4. Finalna struktura produkcyjna
Na VPS:
/opt/corelabtech/
W środku:
app.py
docker-compose.yml
Dockerfile
.env
requirements.txt
routes/
services/
security/
templates/
static/
logs/
monitoring/
nginx/
________________________________________
5. DNS — konfiguracja domeny
Załóżmy:
corelabtech.pl
Aplikacja:
app.corelabtech.pl
________________________________________
5.1. Rekord A
W panelu DNS home.pl dodaj:
Typ: A
Host: app
Wartość: IP_VPS
Opcjonalnie:
Typ: A
Host: @
Wartość: IP_VPS
________________________________________
5.2. Test DNS
Na komputerze:
ping app.corelabtech.pl
lub:
nslookup app.corelabtech.pl
Musi wskazywać IP VPS.
________________________________________
6. Pierwsze logowanie SSH
Dostaniesz:
IP
login
hasło
Połącz się:
ssh root@IP_SERWERA
________________________________________
7. Aktualizacja Ubuntu
apt update && apt upgrade -y
________________________________________
8. Instalacja Dockera
apt install docker.io docker-compose-plugin -y
________________________________________
8.1. Start Dockera
systemctl enable docker
systemctl start docker
________________________________________
8.2. Test Dockera
docker info
Jeżeli działa:
•	VPS jest gotowy. 
________________________________________
9. Instalacja Git
apt install git -y
________________________________________
10. Wgranie projektu
OPCJA REKOMENDOWANA — GitHub
________________________________________
10.1. Lokalnie wrzuć projekt do GitHub
Repo:
private repository
________________________________________
10.2. VPS — clone
cd /opt

git clone https://github.com/TWOJE_REPO/corelabtech.git

cd corelabtech
________________________________________
11. Produkcyjny .env
Utwórz:
nano .env
FINALNA wersja:
SECRET_KEY=SUPER_RANDOM_SECRET_KEY

DATABASE_URL=postgresql://corelabtech_user:SUPER_DB_PASSWORD@db:5432/corelabtech

APP_ENV=production
FLASK_ENV=production

SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SECURE=False

REMEMBER_COOKIE_HTTPONLY=True
________________________________________
12. Zmień SECRET_KEY
Nigdy nie używaj:
SECRET_KEY=test
Generowanie:
python3 -c "import secrets; print(secrets.token_hex(32))"
Wklej do .env.
________________________________________
13. Nie commituj .env
W .gitignore:
.env
________________________________________
14. Zmień hasła demo
Przed produkcją:
❌ Admin123!
❌ Operator123!
❌ Researcher123!
❌ Viewer123!
________________________________________
14.1. Minimalnie
Zmień:
•	admin, 
•	researcher, 
•	operator. 
________________________________________
14.2. Najlepiej
Usuń:
•	viewer demo, 
•	operator demo. 
________________________________________
15. Finalny docker-compose.yml
NA PRODUKCJI:
services:

  web:
    build: .

    container_name: corelabtech_web

    restart: unless-stopped

    ports:
      - "127.0.0.1:5000:5000"

    env_file:
      - .env

    environment:
      DATABASE_URL: postgresql://corelabtech_user:SUPER_DB_PASSWORD@db:5432/corelabtech

    depends_on:
      - db

    volumes:
      - ./logs:/app/logs
      - ./data:/app/data

    command: >
      gunicorn app:app
      --workers 3
      --bind 0.0.0.0:5000
      --timeout 300

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5

    networks:
      - corelabtech_network


  db:
    image: postgres:16

    container_name: corelabtech_postgres

    restart: unless-stopped

    environment:
      POSTGRES_DB: corelabtech
      POSTGRES_USER: corelabtech_user
      POSTGRES_PASSWORD: SUPER_DB_PASSWORD

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U corelabtech_user -d corelabtech"]
      interval: 10s
      timeout: 5s
      retries: 5

    networks:
      - corelabtech_network


networks:
  corelabtech_network:
    driver: bridge


volumes:
  postgres_data:
________________________________________
16. Usuń publiczny PostgreSQL
USUŃ:
ports:
  - "5432:5432"
PostgreSQL ma być:
•	tylko wewnątrz Dockera, 
•	niewidoczny z Internetu. 
________________________________________
17. Publicznie tylko 80 i 443
Publicznie ma działać:
80 HTTP
443 HTTPS
Nie:
•	5000, 
•	
5432.	
________________________________________
18. Build produkcyjny
docker compose build --no-cache
________________________________________
19. Start produkcyjny
docker compose up -d
________________________________________
20. Test kontenerów
docker ps
Powinno być:
corelabtech_web healthy
corelabtech_postgres healthy
________________________________________
21. Init PostgreSQL
docker compose exec web python init_postgres_db.py
________________________________________
22. Seed PostgreSQL
docker compose exec web python seed_postgres_db.py
________________________________________
23. Test health lokalnie
Na VPS:
curl http://127.0.0.1:5000/api/health
________________________________________
24. Instalacja Nginx
apt install nginx -y
________________________________________
25. Finalny config Nginx
Plik:
nano /etc/nginx/sites-available/corelabtech
Wklej:
server {

    listen 80;

    server_name app.corelabtech.pl;

    client_max_body_size 100M;

    location / {

        proxy_pass http://127.0.0.1:5000;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
    }
}
________________________________________
26. Aktywacja Nginx
ln -s /etc/nginx/sites-available/corelabtech /etc/nginx/sites-enabled/
Test:
nginx -t
Reload:
systemctl reload nginx
________________________________________
27. Test domeny
http://app.corelabtech.pl/api/health
________________________________________
28. Firewall UFW
Instalacja:
apt install ufw -y
________________________________________
28.1. Otwórz porty
ufw allow OpenSSH
ufw allow 80
ufw allow 443
________________________________________
28.2. Włącz firewall
ufw enable
________________________________________
28.3. Status
ufw status
________________________________________
29. HTTPS
Instalacja:
apt install certbot python3-certbot-nginx -y
________________________________________
29.1. Certyfikat
certbot --nginx -d app.corelabtech.pl
________________________________________
29.2. Test HTTPS
https://app.corelabtech.pl
https://app.corelabtech.pl/login
https://app.corelabtech.pl/api/health
________________________________________
30. SESSION_COOKIE_SECURE
DOPIERO po HTTPS:
.env
SESSION_COOKIE_SECURE=True
Restart:
docker compose restart web
________________________________________
31. Test login po HTTPS
Sprawdź:
https://app.corelabtech.pl/login
________________________________________
32. Test ról
Sprawdź:
admin
operator
researcher
viewer
________________________________________
32.1. Admin
Powinien widzieć:
•	AI QA, 
•	performance, 
•	debug, 
•	admin tools. 
________________________________________
32.2. Viewer
Nie powinien widzieć:
•	run performance, 
•	debug, 
•	admin tools. 
________________________________________
33. Ukryj performance dla non-admin
W template:
{% if current_user.role == "admin" %}
<button>Run Performance</button>
{% endif %}
________________________________________
34. Zablokuj /debug/db
Route:
@login_required
@role_required("admin")
________________________________________
35. Backup PostgreSQL
Utwórz:
mkdir -p backups
________________________________________
35.1. Backup
docker compose exec -T db pg_dump -U corelabtech_user corelabtech > backups/corelabtech_$(date +%Y%m%d_%H%M%S).sql
________________________________________
36. Automatyczny backup
crontab -e
Dodaj:
0 2 * * * cd /opt/corelabtech && docker compose exec -T db pg_dump -U corelabtech_user corelabtech > backups/corelabtech_$(date +\%Y\%m\%d_\%H\%M\%S).sql
________________________________________
37. Logrotate
Plik:
nano /etc/logrotate.d/corelabtech
________________________________________
37.1. Konfiguracja
/opt/corelabtech/logs/*.log {

    daily
    rotate 14

    compress

    missingok
    notifempty

    copytruncate
}
________________________________________
38. Prometheus
Folder:
monitoring/prometheus.yml
________________________________________
38.1. prometheus.yml
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "corelabtech"
    static_configs:
      - targets: ["web:5000"]
________________________________________
39. Prometheus exporter
requirements.txt
prometheus-flask-exporter
________________________________________
39.1. app.py
from prometheus_flask_exporter import PrometheusMetrics

metrics = PrometheusMetrics(app)

metrics.info(
    "corelabtech_app_info",
    "CoreLabTech application",
    version="1.0.0"
)
________________________________________
40. Test metrics
https://app.corelabtech.pl/metrics
________________________________________
41. Grafana
Docelowo dashboardy:
CPU
RAM
Docker
PostgreSQL
API latency
upload rate
AI usage
HTTP 500
________________________________________
42. Monitoring checklist
Codziennie:
docker ps
docker compose logs web --tail=80
oraz:
/api/health
/metrics
________________________________________
43. Aktualizacja aplikacji
cd /opt/corelabtech

git pull

docker compose build --no-cache

docker compose up -d
________________________________________
44. NIGDY nie rób
docker compose down -v
bo:
•	usunie DB, 
•	usunie dane. 
________________________________________
45. Finalna checklista bezpieczeństwa
✅ SECRET_KEY losowy
✅ demo passwords zmienione
✅ HTTPS działa
✅ SESSION_COOKIE_SECURE=True
✅ PostgreSQL niepubliczny
✅ tylko 80/443 publiczne
✅ firewall działa
✅ /api/health działa
✅ login działa po HTTPS
✅ role działają
✅ backup działa
✅ logrotate działa
✅ .env nie jest w repo
✅ run performance ukryty dla non-admin
✅ /debug/db tylko admin
✅ monitoring działa
________________________________________
46. Finalny status produkcyjny
Po wykonaniu tego tutoriala CoreLabTech będzie mieć:
✅ Docker production
✅ PostgreSQL production
✅ Nginx
✅ HTTPS
✅ Gunicorn
✅ RBAC
✅ CSRF
✅ secure cookies
✅ backupy
✅ firewall
✅ monitoring
✅ Prometheus
✅ Grafana-ready
✅ upload security
✅ AI QA security
✅ production hardening
To już poziom:
production-grade AI telemetry platform



============================================================================
KONIEC ARCHIWUM ŹRÓDŁOWEGO — PLIK 1: Wklejony tekst(68).txt
============================================================================

============================================================================
ARCHIWUM ŹRÓDŁOWE — PLIK 2: Wklejony tekst (2)(3).txt
============================================================================

CoreLabTech — szczegółowy tutorial wdrożenia Auth + RBAC + Security + Docker + Deployment + Monitoring
Ten dokument zakłada, że masz już zakończony etap:
PostgreSQL migration ✅
Playwright 11 passed ✅
Gatling simulations passed ✅
CSV/FIT upload działa ✅
AI analysis działa ✅
/api/sessions działa ✅
/api/user_trends działa ✅
Docker Desktop + WSL2 działa ✅
PostgreSQL container działa ✅
Flask + Gunicorn działa ✅
/api/health działa ✅
Login z CSRF działa ✅
Teraz finalnie porządkujemy:
1. Auth + RBAC ✅
2. Security hardening ✅
3. Docker ✅
4. Production deployment
5. Monitoring
6. Troubleshooting
________________________________________
0. Struktura docelowa projektu po tym etapie
Po zakończeniu prac projekt powinien mieć taką strukturę:
corelabtech_final_clean/
│
├── app.py
├── database_postgres.py
├── init_postgres_db.py
├── seed_postgres_db.py
├── .env
├── .env.example
├── requirements.txt
├── .gitignore
├── .dockerignore
├── Dockerfile
├── docker-compose.yml
├── entrypoint.sh
│
├── auth/
│   ├── __init__.py
│   ├── auth_routes.py
│   ├── decorators.py
│   └── user_model.py
│
├── security/
│   ├── __init__.py
│   ├── csrf.py
│   ├── limiter.py
│   ├── headers.py
│   └── upload_validation.py
│
├── routes/
│   ├── health_routes.py
│   ├── research_routes.py
│   ├── performance_routes.py
│   ├── ai_qa_routes.py
│   ├── qa_routes.py
│   └── ...
│
├── services/
│   ├── logger_service.py
│   ├── performance_service.py
│   ├── fit_parser.py
│   └── ...
│
├── templates/
│   ├── layout.html
│   ├── login.html
│   ├── unauthorized.html
│   ├── profile.html
│   ├── ai_testing_lab_public.html
│   ├── performance_tests.html
│   └── ...
│
├── logs/
│   ├── .gitkeep
│   └── archived/
│       └── .gitkeep
│
├── data/
│   └── uploads/
│       └── temp/
│
├── nginx/
│   └── corelabtech.conf
│
├── monitoring/
│   ├── prometheus.yml
│   └── grafana/
│       └── dashboards/
│
└── tests/
    ├── e2e/
    ├── api/
    ├── ai/
    └── performance/
Ważna zmiana względem starszej wersji:
health_routes.py trzymamy w routes/health_routes.py
a nie w:
monitoring/health_routes.py
Folder monitoring/ zostaje dla:
Prometheus
Grafana
dashboardów
alertów
________________________________________
1. Finalny model dostępu
Publiczne — bez logowania
/
/about
/technology
/contact
/qa
/performance-tests
/ai-testing-lab
/login
/api/health
Ważne:
/performance-tests
publicznie pokazuje tylko opis.
Przyciski:
Run Sessions API Test
Run Merge Test
Run AI Analysis Test
Run CSV Upload Test
Run FIT Upload Test
Run Gatling
są widoczne tylko dla admina.
________________________________________
Po logowaniu
/chamber
/research
/ai-lab
/profile
/logout
/api/sessions
________________________________________
Operator + Admin
/chamber
/upload_csv
/upload_fit
/api/save_phase
/api/save_full_session
/api/during_merge
________________________________________
Researcher + Admin
/ai-qa-lab
/api/run_analysis
/api/user_trends/<user_id>
/api/ai_qa/*
/report
________________________________________
Admin only
/admin
/debug/db
/api/run_playwright
/api/qa/run_pipeline
/api/performance/run
/api/performance/history
/api/performance/latest
/api/users
/api/delete_user
/api/delete_sessions
________________________________________
2. Role RBAC
viewer      → podgląd tylko własnych sesji
operator    → tworzenie sesji HBOT, upload CSV/FIT, merge
researcher  → AI analysis, trendy, eksporty, AI QA Lab
admin       → QA, performance, DB, users, debug, monitoring
Praktyczna interpretacja:
viewer
- widzi tylko własne sesje
- nie tworzy danych
- nie uruchamia AI
- nie odpala QA/performance
operator
- tworzy sesje HBOT
- wrzuca CSV/FIT
- zapisuje PRE/DURING/POST
- uruchamia merge timeline
researcher
- uruchamia AI analysis
- analizuje trendy
- widzi AI QA Lab
- może analizować dane badawcze
admin
- pełna kontrola
- users
- DB debug
- Playwright QA
- Gatling performance
- monitoring
________________________________________
3. ETAP 1 — instalacja pakietów
W katalogu projektu uruchom:
pip install flask-login flask-wtf flask-limiter email-validator flask-cors python-dotenv psycopg2-binary gunicorn fitparse pandas numpy prometheus-flask-exporter
Finalny requirements.txt powinien zawierać minimum:
Flask
flask-cors
flask-login
flask-wtf
flask-limiter
email-validator
python-dotenv
gunicorn
psycopg2-binary
fitparse
pandas
numpy
prometheus-flask-exporter
Dlaczego te pakiety są ważne:
flask-login                → logowanie i sesja użytkownika
flask-wtf                  → CSRF
flask-limiter              → rate limiting
email-validator            → walidacja email
flask-cors                 → jeśli używasz CORS w app.py
python-dotenv              → ładowanie .env
gunicorn                   → produkcyjny serwer WSGI
psycopg2-binary            → PostgreSQL
fitparse                   → parser Garmin FIT
pandas / numpy             → analiza danych
prometheus-flask-exporter  → /metrics dla Prometheus
Po zmianie requirements.txt w Dockerze wykonuj:
docker compose down
docker compose build --no-cache
docker compose up -d
________________________________________
4. ETAP 2 — sprawdzenie tabeli users
W pgAdmin Query Tool albo przez Docker PostgreSQL uruchom:
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name='users'
ORDER BY ordinal_position;
Tabela users powinna mieć minimum:
id
user_id
email
password_hash
role
is_active
Jeśli brakuje pól, wykonaj:
ALTER TABLE users ADD COLUMN IF NOT EXISTS email VARCHAR(255) UNIQUE;
ALTER TABLE users ADD COLUMN IF NOT EXISTS password_hash TEXT;
ALTER TABLE users ADD COLUMN IF NOT EXISTS role VARCHAR(50) DEFAULT 'viewer';
ALTER TABLE users ADD COLUMN IF NOT EXISTS is_active BOOLEAN DEFAULT TRUE;
W Dockerze możesz sprawdzić użytkowników tak:
docker compose exec db psql -U corelabtech_user -d corelabtech -c "SELECT email, role, password_hash IS NOT NULL AS has_password FROM users;"
Poprawny wynik powinien pokazać:
admin@corelabtech.local       admin       true
operator@corelabtech.local    operator    true
researcher@corelabtech.local  researcher  true
viewer@corelabtech.local      viewer      true
________________________________________
5. ETAP 3 — utworzenie folderu auth
Utwórz folder:
auth/
W nim pliki:
auth/__init__.py
auth/user_model.py
auth/decorators.py
auth/auth_routes.py
________________________________________
6. Plik auth/init.py
Lokalizacja:
auth/__init__.py
Zawartość:
# Auth package for CoreLabTech
________________________________________
7. Plik auth/user_model.py
Lokalizacja:
auth/user_model.py
Finalna zawartość:
from flask_login import UserMixin
from database_postgres import db


class User(UserMixin):

    def __init__(self, id, user_id, email, password_hash, role, is_active=True):
        self.id = str(id)
        self.user_id = user_id
        self.email = email
        self.password_hash = password_hash
        self.role = role or "viewer"
        self._is_active = is_active

    @property
    def is_active(self):
        return bool(self._is_active)


def row_to_user(row):

    if not row:
        return None

    return User(
        id=row[0],
        user_id=row[1],
        email=row[2],
        password_hash=row[3],
        role=row[4],
        is_active=row[5]
    )


def get_user_by_id(id):

    con = db()
    c = con.cursor()

    c.execute("""
        SELECT id, user_id, email, password_hash, role, is_active
        FROM users
        WHERE id=%s
        LIMIT 1
    """, (id,))

    row = c.fetchone()

    c.close()
    con.close()

    return row_to_user(row)


def get_user_by_email(email):

    con = db()
    c = con.cursor()

    c.execute("""
        SELECT id, user_id, email, password_hash, role, is_active
        FROM users
        WHERE email=%s
        LIMIT 1
    """, (email,))

    row = c.fetchone()

    c.close()
    con.close()

    return row_to_user(row)
Pułapka, która wystąpiła wcześniej:
NameError: name 'row' is not defined
Powód:
return row_to_user(row)
było poza funkcją albo przed zdefiniowaniem row.
________________________________________
8. Plik auth/decorators.py
Lokalizacja:
auth/decorators.py
Zawartość:
from functools import wraps
from flask import abort
from flask_login import current_user


def role_required(*allowed_roles):

    def decorator(fn):

        @wraps(fn)
        def wrapper(*args, **kwargs):

            if not current_user.is_authenticated:
                abort(401)

            if current_user.role not in allowed_roles:
                abort(403)

            return fn(*args, **kwargs)

        return wrapper

    return decorator
Użycie:
@login_required
@role_required("admin")
def admin_panel():
    ...
albo:
@login_required
@role_required("researcher", "admin")
def ai_qa_lab():
    ...
________________________________________
9. Plik auth/auth_routes.py
Lokalizacja:
auth/auth_routes.py
Finalna wersja produkcyjna:
from flask import Blueprint, render_template, request, redirect, flash
from flask_login import login_user, logout_user, login_required, current_user
from werkzeug.security import check_password_hash

from auth.user_model import get_user_by_email
from security.limiter import limiter


auth_bp = Blueprint("auth", __name__)


@auth_bp.route("/login", methods=["GET", "POST"])
@limiter.limit("5 per minute")
def login():

    if current_user.is_authenticated:
        return redirect("/chamber")

    if request.method == "POST":

        email = request.form.get("email")
        password = request.form.get("password")

        user = get_user_by_email(email)

        if not user:
            flash("Invalid email or password")
            return render_template("login.html")

        if not user.password_hash:
            flash("Account has no password configured")
            return render_template("login.html")

        if not check_password_hash(user.password_hash, password):
            flash("Invalid email or password")
            return render_template("login.html")

        if not user.is_active:
            flash("Account is inactive")
            return render_template("login.html")

        login_user(user)

        return redirect("/chamber")

    return render_template("login.html")


@auth_bp.route("/logout")
@login_required
def logout():

    logout_user()

    return redirect("/login")


@auth_bp.route("/profile")
@login_required
def profile():

    return render_template("profile.html")
Ważne:
Nie dawaj @csrf.exempt na /login.
Login powinien być chroniony przez CSRF.
________________________________________
10. ETAP 4 — template logowania
10.1. Plik templates/login.html
Lokalizacja:
templates/login.html
Finalna wersja:
{% extends "layout.html" %}

{% block content %}

<div class="auth-wrapper">

    <div class="auth-panel">

        <h1 class="auth-title">
            CoreLabTech Login
        </h1>

        <p class="auth-subtitle">
            Secure access to HBOT Research Platform
        </p>

        <!-- FLASH MESSAGES -->

        {% with messages = get_flashed_messages() %}
            {% if messages %}

                <div class="auth-messages">

                    {% for message in messages %}
                        <div class="auth-message">
                            {{ message }}
                        </div>
                    {% endfor %}

                </div>

            {% endif %}
        {% endwith %}

        <!-- LOGIN FORM -->

        <form
            method="POST"
            action="{{ url_for('auth.login') }}"
            class="auth-form"
        >

            <!-- CSRF -->

            <input
                type="hidden"
                name="csrf_token"
                value="{{ csrf_token() }}"
            >

            <!-- EMAIL -->

            <label for="email">
                Email
            </label>

            <input
                type="email"
                id="email"
                name="email"
                placeholder="admin@corelabtech.local"
                required
                autocomplete="email"
            >

            <!-- PASSWORD -->

            <label for="password">
                Password
            </label>

            <input
                type="password"
                id="password"
                name="password"
                placeholder="Password"
                required
                autocomplete="current-password"
            >

            <!-- SUBMIT -->

            <button
                type="submit"
                class="auth-button"
            >
                Login
            </button>

        </form>

        <!-- DEMO ACCOUNTS -->

        <div class="demo-accounts">

            <h3>Demo Accounts</h3>

            <div class="demo-box">

                <p>
                    <strong>Admin:</strong><br>
                    admin@corelabtech.local<br>
                    Admin123!
                </p>

                <p>
                    <strong>Researcher:</strong><br>
                    researcher@corelabtech.local<br>
                    Researcher123!
                </p>

                <p>
                    <strong>Operator:</strong><br>
                    operator@corelabtech.local<br>
                    Operator123!
                </p>

                <p>
                    <strong>Viewer:</strong><br>
                    viewer@corelabtech.local<br>
                    Viewer123!
                </p>

            </div>

        </div>

    </div>

</div>

{% endblock %}
Najważniejszy element:
<input
    type="hidden"
    name="csrf_token"
    value="{{ csrf_token() }}"
>
Bez niego pojawi się błąd:
Bad Request
The CSRF token is missing.
________________________________________
10.2. Plik templates/unauthorized.html
Lokalizacja:
templates/unauthorized.html
Zawartość:
{% extends "layout.html" %}

{% block content %}

<h1>Access denied</h1>

<p>You do not have permission to access this page.</p>

<a href="/">
    <button>Back to home</button>
</a>

{% endblock %}
________________________________________
10.3. Plik templates/profile.html
Lokalizacja:
templates/profile.html
Zawartość:
{% extends "layout.html" %}

{% block content %}

<h1>Profile</h1>

<div class="panel">
    <p><strong>Email:</strong> {{ current_user.email }}</p>
    <p><strong>User ID:</strong> {{ current_user.user_id }}</p>
    <p><strong>Role:</strong> {{ current_user.role }}</p>
</div>

<a href="/logout">
    <button>Logout</button>
</a>

{% endblock %}
________________________________________
11. ETAP 5 — aktualizacja app.py
Otwórz:
app.py
11.1. Dodaj importy na górze
Dodaj:
import os

from flask import render_template, redirect, url_for
from flask_login import LoginManager

from auth.auth_routes import auth_bp
from auth.user_model import get_user_by_id

from security.csrf import csrf
from security.limiter import limiter
from security.headers import configure_security_headers

from routes.health_routes import health_bp
Jeśli masz już część importów, nie duplikuj ich.
________________________________________
11.2. Po utworzeniu app = Flask(__name__) dodaj konfigurację
Kolejność ma znaczenie.
app = Flask(__name__)

app.secret_key = os.getenv(
    "SECRET_KEY",
    "dev-secret-change-me"
)

app.config["SESSION_COOKIE_HTTPONLY"] = True
app.config["REMEMBER_COOKIE_HTTPONLY"] = True

app.config["SESSION_COOKIE_SECURE"] = (
    os.getenv("SESSION_COOKIE_SECURE", "False") == "True"
)
Ważne:
app.secret_key musi być ustawiony przed csrf.init_app(app)
________________________________________
11.3. Dodaj security init
Bezpośrednio po konfiguracji cookies:
configure_security_headers(app)

csrf.init_app(app)

limiter.init_app(app)
Finalna kolejność:
app = Flask(__name__)

app.secret_key = os.getenv(
    "SECRET_KEY",
    "dev-secret-change-me"
)

app.config["SESSION_COOKIE_HTTPONLY"] = True
app.config["REMEMBER_COOKIE_HTTPONLY"] = True
app.config["SESSION_COOKIE_SECURE"] = (
    os.getenv("SESSION_COOKIE_SECURE", "False") == "True"
)

configure_security_headers(app)

csrf.init_app(app)

limiter.init_app(app)
________________________________________
11.4. Dodaj LoginManager
Po konfiguracji app dodaj:
login_manager = LoginManager()

login_manager.init_app(app)

login_manager.login_view = "auth.login"
login_manager.login_message = "Please log in to access this page."


@login_manager.user_loader
def load_user(user_id):
    return get_user_by_id(user_id)
________________________________________
11.5. Zarejestruj auth blueprint
W miejscu gdzie rejestrujesz blueprinty dodaj:
app.register_blueprint(auth_bp)
Zarejestruj też health blueprint:
app.register_blueprint(health_bp)
________________________________________
11.6. Dodaj obsługę błędów
Na końcu konfiguracji app.py dodaj:
@app.errorhandler(403)
def forbidden(e):
    return render_template("unauthorized.html"), 403


@app.errorhandler(401)
def unauthorized(e):
    return redirect(url_for("auth.login"))
________________________________________
12. ETAP 6 — aktualizacja .env
Otwórz:
.env
Finalna wersja lokalna:
SECRET_KEY=corelabtech-super-secret-key-change-this

DATABASE_URL=postgresql://corelabtech_user:corelabtech_password@localhost:5432/corelabtech

APP_ENV=development
FLASK_ENV=development

SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SECURE=False
REMEMBER_COOKIE_HTTPONLY=True
W Docker Compose DATABASE_URL zostanie nadpisany na:
postgresql://corelabtech_user:corelabtech_password@db:5432/corelabtech
Na produkcji po HTTPS ustawisz:
APP_ENV=production
FLASK_ENV=production
SESSION_COOKIE_SECURE=True
Uwaga:
SESSION_COOKIE_SECURE=True wymaga HTTPS.
Lokalnie bez HTTPS zostaw SESSION_COOKIE_SECURE=False.
________________________________________
13. ETAP 7 — aktualizacja seed_postgres_db.py
Sprawdź, czy demo users mają hasła.
Plik:
seed_postgres_db.py
Powinien tworzyć:
admin@corelabtech.local / Admin123!
operator@corelabtech.local / Operator123!
researcher@corelabtech.local / Researcher123!
viewer@corelabtech.local / Viewer123!
Jeśli już wykonałeś seed wcześniej, a użytkownicy nie mają password_hash, sprawdź:
SELECT email, role, password_hash IS NOT NULL AS has_password
FROM users;
W Dockerze:
docker compose exec db psql -U corelabtech_user -d corelabtech -c "SELECT email, role, password_hash IS NOT NULL AS has_password FROM users;"
Jeżeli has_password=false, uruchom:
docker compose exec web python seed_postgres_db.py
Jeśli ON CONFLICT DO NOTHING nie aktualizuje haseł, zmień w seedzie na:
ON CONFLICT (user_id)
DO UPDATE SET
    email = EXCLUDED.email,
    password_hash = EXCLUDED.password_hash,
    role = EXCLUDED.role,
    is_active = EXCLUDED.is_active
Poprawny wynik seedowania w Dockerze:
PostgreSQL seed completed
Admin:
  admin@corelabtech.local / Admin123!
Operator:
  operator@corelabtech.local / Operator123!
Researcher:
  researcher@corelabtech.local / Researcher123!
Viewer:
  viewer@corelabtech.local / Viewer123!


14. ETAP 8 — zabezpieczenie research_routes.py
Otwórz plik:
routes/research_routes.py
Na górze pliku dodaj importy:
from flask_login import login_required, current_user
from auth.decorators import role_required
from security.limiter import limiter
from security.csrf import csrf
from security.upload_validation import (
    validate_extension,
    safe_upload_filename,
    validate_file_size
)
Jeśli część importów już istnieje, nie duplikuj ich.
________________________________________
14.1. /chamber
Było:
@research_bp.route("/chamber")
def chamber_testing():
    return render_template("chamber_testing.html")
Ma być:
@research_bp.route("/chamber")
@login_required
@role_required("operator", "admin")
def chamber_testing():
    return render_template("chamber_testing.html")
________________________________________
14.2. /research
@research_bp.route("/research")
@login_required
@role_required("viewer", "operator", "researcher", "admin")
def research_dashboard():
    return render_template("research_dashboard.html")
________________________________________
14.3. /admin
@research_bp.route("/admin")
@login_required
@role_required("admin")
def admin_panel():
    return render_template("admin_panel.html")
________________________________________
14.4. /upload_csv
Endpoint API/upload powinien być chroniony przez:
login_required
role_required
rate limit
upload validation
csrf.exempt
Finalny układ dekoratorów:
@csrf.exempt
@research_bp.route("/upload_csv", methods=["POST"])
@login_required
@role_required("operator", "admin")
@limiter.limit("20 per hour")
def upload_csv():
Uwaga:
@csrf.exempt jest tutaj dopuszczalne, bo to endpoint upload/API,
a nie klasyczny formularz HTML.
________________________________________
14.5. /upload_fit
@csrf.exempt
@research_bp.route("/upload_fit", methods=["POST"])
@login_required
@role_required("operator", "admin")
@limiter.limit("20 per hour")
def upload_fit():
________________________________________
14.6. /api/save_phase
@csrf.exempt
@research_bp.route("/api/save_phase", methods=["POST"])
@login_required
@role_required("operator", "admin")
def save_phase():
________________________________________
14.7. /api/save_full_session
@csrf.exempt
@research_bp.route("/api/save_full_session", methods=["POST"])
@login_required
@role_required("operator", "admin")
def save_full_session():
________________________________________
14.8. /api/during_merge
@csrf.exempt
@research_bp.route("/api/during_merge", methods=["POST"])
@login_required
@role_required("operator", "researcher", "admin")
def during_merge():
________________________________________
14.9. /api/run_analysis
@csrf.exempt
@research_bp.route("/api/run_analysis", methods=["POST"])
@login_required
@role_required("researcher", "admin")
@limiter.limit("60 per hour")
def run_analysis():
________________________________________
14.10. /api/users
@research_bp.route("/api/users", methods=["GET", "POST"])
@login_required
@role_required("admin")
def users():
________________________________________
14.11. /api/delete_user
@csrf.exempt
@research_bp.route("/api/delete_user", methods=["POST"])
@login_required
@role_required("admin")
def delete_user():
________________________________________
14.12. /api/delete_sessions
@csrf.exempt
@research_bp.route("/api/delete_sessions", methods=["POST"])
@login_required
@role_required("admin")
def delete_sessions():
________________________________________
14.13. /api/run_playwright
@csrf.exempt
@research_bp.route("/api/run_playwright", methods=["POST"])
@login_required
@role_required("admin")
@limiter.limit("5 per hour")
def run_playwright():
________________________________________
14.14. /api/generate_test
@csrf.exempt
@research_bp.route("/api/generate_test", methods=["POST"])
@login_required
@role_required("admin", "researcher")
def generate_test():
________________________________________
14.15. /debug/db
@research_bp.route("/debug/db")
@login_required
@role_required("admin")
def debug_db():
________________________________________
15. ETAP 9 — filtrowanie /api/sessions
W routes/research_routes.py znajdź funkcję:
def sessions():
Finalna wersja:
@research_bp.route("/api/sessions")
@login_required
def sessions():

    con = db()
    c = con.cursor()

    if current_user.role in ["admin", "researcher"]:

        c.execute("""
            SELECT
                session_id,
                COALESCE(user_id, 'UNKNOWN') as user_id,
                completed,
                created_at
            FROM full_sessions
            ORDER BY created_at DESC
        """)

    else:

        c.execute("""
            SELECT
                session_id,
                COALESCE(user_id, 'UNKNOWN') as user_id,
                completed,
                created_at
            FROM full_sessions
            WHERE user_id=%s
            ORDER BY created_at DESC
        """, (
            current_user.user_id,
        ))

    rows = c.fetchall()

    c.close()
    con.close()

    data = []

    for r in rows:

        data.append({
            "session_id": r[0],
            "user_id": r[1],
            "completed": r[2],
            "date": r[3],
        })

    return jsonify(data)
Logika:
admin      → widzi wszystkie sesje
researcher → widzi wszystkie sesje
operator   → widzi tylko własne sesje
viewer     → widzi tylko własne sesje
________________________________________
16. ETAP 10 — zabezpieczenie /api/user_trends/<user_id>
W routes/research_routes.py znajdź funkcję:
def user_trends(user_id):
Finalna wersja dekoratorów:
@research_bp.route("/api/user_trends/<user_id>")
@login_required
@role_required("viewer", "operator", "researcher", "admin")
def user_trends(user_id):
Na początku funkcji dodaj:
    if current_user.role not in ["admin", "researcher"]:
        if current_user.user_id != user_id:
            return jsonify({"error": "forbidden"}), 403
Przykład:
@research_bp.route("/api/user_trends/<user_id>")
@login_required
@role_required("viewer", "operator", "researcher", "admin")
def user_trends(user_id):

    if current_user.role not in ["admin", "researcher"]:
        if current_user.user_id != user_id:
            return jsonify({"error": "forbidden"}), 403

    con = db()
    c = con.cursor()

    c.execute("""
        SELECT
            session_id,
            ai_score,
            risk_level,
            created_at
        FROM ai_results
        WHERE user_id=%s
        ORDER BY created_at DESC
    """, (
        user_id,
    ))

    rows = c.fetchall()

    c.close()
    con.close()

    trends = []

    for r in rows:
        trends.append({
            "session_id": r[0],
            "ai_score": r[1],
            "risk_level": r[2],
            "created_at": r[3],
        })

    return jsonify({
        "user_id": user_id,
        "trends": trends
    })
Jeśli Twoja tabela AI ma inne kolumny, zostaw swoją logikę SQL, ale zachowaj:
@login_required
@role_required("viewer", "operator", "researcher", "admin")
oraz kontrolę:
if current_user.role not in ["admin", "researcher"]:
    if current_user.user_id != user_id:
        return jsonify({"error": "forbidden"}), 403
________________________________________
17. ETAP 11 — aktualizacja layout.html
Otwórz:
templates/layout.html
W menu dodaj warunki widoczności linków.
Publiczne linki
<a href="/">Home</a>
<a href="/about">About</a>
<a href="/technology">Technology</a>
<a href="/qa">QA</a>
<a href="/ai-testing-lab">AI Testing Lab</a>
<a href="/performance-tests">Performance</a>
<a href="/contact">Contact</a>
________________________________________
Niezalogowany
{% if not current_user.is_authenticated %}
    <a href="/login">Login</a>
{% endif %}
________________________________________
Zalogowany
{% if current_user.is_authenticated %}
    <a href="/chamber">Chamber</a>
    <a href="/research">Research</a>
    <a href="/ai-lab">AI Lab</a>
    <a href="/profile">Profile</a>
    <a href="/logout">Logout</a>
{% endif %}
________________________________________
Researcher/Admin
{% if current_user.is_authenticated and current_user.role in ["researcher", "admin"] %}
    <a href="/ai-qa-lab">AI QA Lab</a>
{% endif %}
________________________________________
Admin
{% if current_user.is_authenticated and current_user.role == "admin" %}
    <a href="/admin">Admin</a>
    <a href="/debug/db">DB Debug</a>
{% endif %}
________________________________________
Finalny przykład sekcji <nav>
<nav>

    <a href="/">Home</a>
    <a href="/about">About</a>
    <a href="/technology">Technology</a>
    <a href="/qa">QA</a>
    <a href="/ai-testing-lab">AI Testing Lab</a>
    <a href="/performance-tests">Performance</a>
    <a href="/contact">Contact</a>

    {% if not current_user.is_authenticated %}

        <a href="/login">Login</a>

    {% endif %}

    {% if current_user.is_authenticated %}

        <a href="/chamber">Chamber</a>
        <a href="/research">Research</a>
        <a href="/ai-lab">AI Lab</a>
        <a href="/profile">Profile</a>
        <a href="/logout">Logout</a>

    {% endif %}

    {% if current_user.is_authenticated and current_user.role in ["researcher", "admin"] %}

        <a href="/ai-qa-lab">AI QA Lab</a>

    {% endif %}

    {% if current_user.is_authenticated and current_user.role == "admin" %}

        <a href="/admin">Admin</a>
        <a href="/debug/db">DB Debug</a>

    {% endif %}

</nav>
________________________________________
18. ETAP 12 — publiczny AI Testing Lab
Utwórz template:
templates/ai_testing_lab_public.html
Zawartość:
{% extends "layout.html" %}

{% block content %}

<h1>AI Testing Lab</h1>

<p>
AI Testing Lab presents the concept of AI-assisted software quality validation
for telemetry, HBOT sessions and AI scoring pipelines.
</p>

<div class="panel">
    <h3>What it validates</h3>
    <ul>
        <li>AI response contract</li>
        <li>Score, risk level and anomaly fields</li>
        <li>Timeline consistency</li>
        <li>Physiological feature extraction</li>
        <li>Research-only disclaimer</li>
    </ul>
</div>

<div class="panel">
    <h3>Example Gherkin</h3>
    <pre>
Feature: HBOT AI QA validation
Scenario: Validate AI analysis contract
Given a completed HBOT session exists
When the AI analysis endpoint is executed
Then the response should contain score, risk_level and features
    </pre>
</div>

<div class="panel">
    <h3>Access</h3>
    <p>
The operational AI QA Lab is available only for researcher and admin roles.
    </p>
</div>

{% endblock %}
Dodaj route np. w research_routes.py:
@research_bp.route("/ai-testing-lab")
def ai_testing_lab_public():
    return render_template("ai_testing_lab_public.html")
Logika:
/ai-testing-lab → publiczny opis
/ai-qa-lab      → prawdziwy panel roboczy po loginie dla researcher/admin
________________________________________
19. ETAP 13 — zabezpieczenie /ai-qa-lab
Znajdź route dla:
/ai-qa-lab
W pliku:
routes/ai_qa_routes.py
Dodaj importy:
from flask_login import login_required
from auth.decorators import role_required
from security.limiter import limiter
from security.csrf import csrf
Finalnie:
@ai_qa_bp.route("/ai-qa-lab")
@login_required
@role_required("researcher", "admin")
def ai_qa_lab():
    return render_template("ai_qa_lab.html")
Endpointy API AI QA, które wykonują akcje, zabezpieczaj tak:
@csrf.exempt
@ai_qa_bp.route("/api/ai_qa/run_test", methods=["POST"])
@login_required
@role_required("researcher", "admin")
def run_test():
Dla pipeline:
@csrf.exempt
@ai_qa_bp.route("/api/qa/run_pipeline", methods=["POST"])
@login_required
@role_required("admin")
@limiter.limit("5 per hour")
def run_pipeline():
________________________________________
20. ETAP 14 — performance-tests public/admin split
Otwórz:
templates/performance_tests.html
Znajdź sekcję z przyciskami:
<button onclick="runPerf('sessions')">...</button>
Opakuj ją:
{% if current_user.is_authenticated and current_user.role == "admin" %}

<div class="qa-actions">

    <button onclick="runPerf('sessions')">
        Run Sessions API Test
    </button>

    <button onclick="runPerf('merge')">
        Run Merge Test
    </button>

    <button onclick="runPerf('analysis')">
        Run AI Analysis Test
    </button>

    <button onclick="runPerf('csv')">
        Run CSV Upload Test
    </button>

    <button onclick="runPerf('fit')">
        Run FIT Upload Test
    </button>

</div>

{% else %}

<div class="qa-card">
    <h3>Performance execution locked</h3>
    <p>
        Public users can review the performance testing architecture,
        but only administrators can execute Gatling tests.
    </p>
</div>

{% endif %}
Ważne:
Publiczny użytkownik widzi opis performance-tests.
Admin widzi przyciski uruchamiania testów.
________________________________________
21. ETAP 15 — zabezpieczenie performance API
W pliku:
routes/performance_routes.py
Dodaj importy:
from flask_login import login_required
from auth.decorators import role_required
from security.limiter import limiter
from security.csrf import csrf
Finalna wersja endpointów:
@csrf.exempt
@performance_bp.route("/api/performance/run", methods=["POST"])
@login_required
@role_required("admin")
@limiter.limit("10 per hour")
def run_performance():
    data = request.json or {}

    test_type = data.get("type")

    result = run_gatling_test(test_type)

    return jsonify(result)
@performance_bp.route("/api/performance/latest")
@login_required
@role_required("admin")
def performance_latest():
    return jsonify(get_latest_performance_result())
@performance_bp.route("/api/performance/history")
@login_required
@role_required("admin")
def performance_history():
    return jsonify({
        "status": "ok",
        "history": get_performance_history()
    })
________________________________________
22. ETAP 16 — security folder
Utwórz:
security/
├── __init__.py
├── upload_validation.py
├── limiter.py
├── headers.py
└── csrf.py
________________________________________
23. Plik security/init.py
Lokalizacja:
security/__init__.py
Zawartość:
# Security helpers for CoreLabTech
________________________________________
24. Pliki security
24.1. security/upload_validation.py
import os
from werkzeug.utils import secure_filename


def validate_extension(filename, allowed_extensions):

    if not filename:
        return False

    if "." not in filename:
        return False

    ext = filename.rsplit(".", 1)[1].lower()

    return ext in allowed_extensions


def safe_upload_filename(filename):

    return secure_filename(filename)


def validate_file_size(file, max_bytes):

    file.seek(0, os.SEEK_END)
    size = file.tell()
    file.seek(0)

    return size <= max_bytes
________________________________________
24.2. security/limiter.py
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address


limiter = Limiter(
    key_func=get_remote_address,

    default_limits=[
        "500 per day",
        "100 per hour"
    ],

    storage_uri="memory://"
)
Produkcja rozszerzona później:
storage_uri="redis://redis:6379"
________________________________________
24.3. security/csrf.py
from flask_wtf.csrf import CSRFProtect


csrf = CSRFProtect()
________________________________________
24.4. security/headers.py
from flask import Flask


def configure_security_headers(app: Flask):

    @app.after_request
    def apply_security_headers(response):

        response.headers["X-Content-Type-Options"] = "nosniff"

        response.headers["X-Frame-Options"] = "SAMEORIGIN"

        response.headers["X-XSS-Protection"] = "1; mode=block"

        response.headers["Referrer-Policy"] = "strict-origin-when-cross-origin"

        response.headers["Content-Security-Policy"] = (
            "default-src 'self'; "
            "img-src 'self' data:; "
            "style-src 'self' 'unsafe-inline'; "
            "script-src 'self' 'unsafe-inline' 'unsafe-eval'; "
            "font-src 'self' data:;"
        )

        return response
________________________________________
25. ETAP 17 — użycie upload validation w research_routes.py
Dodaj import:
from security.upload_validation import (
    validate_extension,
    safe_upload_filename,
    validate_file_size
)
________________________________________
25.1. CSV upload validation
W upload_csv() przed zapisem pliku:
if not validate_extension(file.filename, {"csv"}):
    return jsonify({"error": "invalid CSV extension"}), 400

if not validate_file_size(file, 20 * 1024 * 1024):
    return jsonify({"error": "CSV file too large"}), 400

filename = safe_upload_filename(file.filename.lower())
Finalny schemat:
@csrf.exempt
@research_bp.route("/upload_csv", methods=["POST"])
@login_required
@role_required("operator", "admin")
@limiter.limit("20 per hour")
def upload_csv():

    file = request.files.get("file")
    session_id = request.form.get("session_id")

    if not file or not session_id:
        return jsonify({"error": "missing file or session_id"}), 400

    if not validate_extension(file.filename, {"csv"}):
        return jsonify({"error": "invalid CSV extension"}), 400

    if not validate_file_size(file, 20 * 1024 * 1024):
        return jsonify({"error": "CSV file too large"}), 400

    filename = safe_upload_filename(file.filename.lower())

    user_id = session_id.split("_")[0] if "_" in session_id else session_id

    os.makedirs("data/uploads/temp", exist_ok=True)

    temp_path = f"data/uploads/temp/{uuid.uuid4()}_{filename}"

    file.save(temp_path)

    try:
        rows = parse_csv_file(temp_path)

        con = db()
        c = con.cursor()

        c.execute("""
            INSERT INTO users (
                user_id,
                subject_id,
                role,
                is_active,
                notes
            )
            VALUES (%s, %s, %s, %s, %s)
            ON CONFLICT (user_id) DO NOTHING
        """, (
            user_id,
            user_id,
            "operator",
            True,
            "Auto-created during CSV upload"
        ))

        for r in rows:
            c.execute("""
                INSERT INTO csv_data (
                    session_id,
                    user_id,
                    timestamp,
                    pulse,
                    heart_rate,
                    spo2,
                    source,
                    filename,
                    raw_json
                )
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (
                session_id,
                user_id,
                str(r.get("timestamp")) if r.get("timestamp") else None,
                r.get("pulse"),
                r.get("heart_rate") or r.get("pulse"),
                r.get("spo2"),
                r.get("source", "csv"),
                filename,
                json.dumps(r, default=str),
            ))

        con.commit()

        c.close()
        con.close()

        return jsonify({
            "status": "csv_saved",
            "records": len(rows),
            "session_id": session_id,
            "user_id": user_id,
            "file": filename,
        })

    except Exception as e:
        traceback.print_exc()
        return jsonify({"error": str(e)}), 500

    finally:
        if os.path.exists(temp_path):
            os.remove(temp_path)
________________________________________
25.2. FIT upload validation
W upload_fit() przed zapisem:
if not validate_extension(file.filename, {"fit"}):
    return jsonify({"error": "invalid FIT extension"}), 400

if not validate_file_size(file, 100 * 1024 * 1024):
    return jsonify({"error": "FIT file too large"}), 400

filename = safe_upload_filename(file.filename.lower())
Finalny schemat:
@csrf.exempt
@research_bp.route("/upload_fit", methods=["POST"])
@login_required
@role_required("operator", "admin")
@limiter.limit("20 per hour")
def upload_fit():

    file = request.files.get("file")
    session_id = request.form.get("session_id")

    if not file or not session_id:
        return jsonify({"error": "missing file or session_id"}), 400

    if not validate_extension(file.filename, {"fit"}):
        return jsonify({"error": "invalid FIT extension"}), 400

    if not validate_file_size(file, 100 * 1024 * 1024):
        return jsonify({"error": "FIT file too large"}), 400

    filename = safe_upload_filename(file.filename.lower())

    user_id = session_id.split("_")[0] if "_" in session_id else session_id

    os.makedirs("data/uploads/temp", exist_ok=True)

    temp_path = f"data/uploads/temp/{uuid.uuid4()}_{filename}"

    file.save(temp_path)

    try:
        rows = parse_fit_file(temp_path)

        con = db()
        c = con.cursor()

        c.execute("""
            INSERT INTO users (
                user_id,
                subject_id,
                role,
                is_active,
                notes
            )
            VALUES (%s, %s, %s, %s, %s)
            ON CONFLICT (user_id) DO NOTHING
        """, (
            user_id,
            user_id,
            "operator",
            True,
            "Auto-created during FIT upload"
        ))

        for r in rows:
            c.execute("""
                INSERT INTO fit_data (
                    session_id,
                    user_id,
                    timestamp,
                    heart_rate,
                    pulse,
                    hr,
                    spo2,
                    rr_interval,
                    hrv,
                    source,
                    filename,
                    raw_json
                )
                VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s)
            """, (
                session_id,
                user_id,
                str(r.get("timestamp")) if r.get("timestamp") else None,
                r.get("heart_rate"),
                r.get("pulse"),
                r.get("heart_rate") or r.get("pulse"),
                r.get("spo2"),
                r.get("rr_interval"),
                r.get("hrv"),
                r.get("source", "fit"),
                filename,
                json.dumps(r, default=str),
            ))

        con.commit()

        c.close()
        con.close()

        return jsonify({
            "status": "fit_saved",
            "records": len(rows),
            "session_id": session_id,
            "user_id": user_id,
            "file": filename,
        })

    except Exception as e:
        traceback.print_exc()
        return jsonify({"error": str(e)}), 500

    finally:
        if os.path.exists(temp_path):
            os.remove(temp_path)
________________________________________
26. ETAP 18 — rate limiting
26.1. Finalna architektura
Nie trzymaj limitera bezpośrednio w app.py.
Finalny układ:
app.py
    ↓
security/limiter.py
    ↓
routes/*
    ↓
@limiter.limit(...)
________________________________________
26.2. Plik security/limiter.py
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address


limiter = Limiter(
    key_func=get_remote_address,

    default_limits=[
        "500 per day",
        "100 per hour"
    ],

    storage_uri="memory://"
)
________________________________________
26.3. app.py
Dodaj import:
from security.limiter import limiter
Po utworzeniu app i konfiguracji security:
limiter.init_app(app)
Finalna kolejność:
app = Flask(__name__)

app.secret_key = os.getenv(
    "SECRET_KEY",
    "dev-secret-change-me"
)

configure_security_headers(app)

csrf.init_app(app)

limiter.init_app(app)
________________________________________
26.4. Gdzie dodawać finalne limity
Dodajesz je bezpośrednio nad konkretnymi funkcjami endpointów.
Login
Plik:
auth/auth_routes.py
@auth_bp.route("/login", methods=["GET", "POST"])
@limiter.limit("5 per minute")
def login():
________________________________________
CSV upload
Plik:
routes/research_routes.py
@csrf.exempt
@research_bp.route("/upload_csv", methods=["POST"])
@login_required
@role_required("operator", "admin")
@limiter.limit("20 per hour")
def upload_csv():
________________________________________
FIT upload
@csrf.exempt
@research_bp.route("/upload_fit", methods=["POST"])
@login_required
@role_required("operator", "admin")
@limiter.limit("20 per hour")
def upload_fit():
________________________________________
AI analysis
@csrf.exempt
@research_bp.route("/api/run_analysis", methods=["POST"])
@login_required
@role_required("researcher", "admin")
@limiter.limit("60 per hour")
def run_analysis():
________________________________________
Playwright pipeline
@csrf.exempt
@ai_qa_bp.route("/api/qa/run_pipeline", methods=["POST"])
@login_required
@role_required("admin")
@limiter.limit("5 per hour")
def run_pipeline():
________________________________________
Performance run
@csrf.exempt
@performance_bp.route("/api/performance/run", methods=["POST"])
@login_required
@role_required("admin")
@limiter.limit("10 per hour")
def run_performance():
________________________________________
26.5. Pułapka
Jeśli dostaniesz:
NameError: name 'limiter' is not defined
to w danym pliku route brakuje:
from security.limiter import limiter
Jeśli dostaniesz:
NameError: name 'csrf' is not defined
to brakuje:
from security.csrf import csrf


26. ETAP 18 — rate limiting
26.1. Finalna architektura
Nie trzymaj limitera bezpośrednio w app.py.
Finalny układ:
app.py
    ↓
security/limiter.py
    ↓
routes/*
    ↓
@limiter.limit(...)
________________________________________
26.2. Plik security/limiter.py
Lokalizacja:
security/limiter.py
Zawartość:
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address


limiter = Limiter(
    key_func=get_remote_address,

    default_limits=[
        "500 per day",
        "100 per hour"
    ],

    storage_uri="memory://"
)
Na późniejszą produkcję można zmienić:
storage_uri="redis://redis:6379"
________________________________________
26.3. Aktualizacja app.py
Usuń stare importy, jeśli masz:
from flask_limiter import Limiter
from flask_limiter.util import get_remote_address
Usuń też starą konfigurację:
limiter = Limiter(...)
Dodaj:
from security.limiter import limiter
Po utworzeniu app = Flask(__name__) i po konfiguracji secret_key / CSRF dodaj:
limiter.init_app(app)
Finalna kolejność:
app = Flask(__name__)

app.secret_key = os.getenv(
    "SECRET_KEY",
    "dev-secret-change-me"
)

configure_security_headers(app)

csrf.init_app(app)

limiter.init_app(app)
________________________________________
26.4. Finalne limity — gdzie dodać
/login
Plik:
auth/auth_routes.py
@auth_bp.route("/login", methods=["GET", "POST"])
@limiter.limit("5 per minute")
def login():
________________________________________
/upload_csv
Plik:
routes/research_routes.py
@csrf.exempt
@research_bp.route("/upload_csv", methods=["POST"])
@login_required
@role_required("operator", "admin")
@limiter.limit("20 per hour")
def upload_csv():
________________________________________
/upload_fit
@csrf.exempt
@research_bp.route("/upload_fit", methods=["POST"])
@login_required
@role_required("operator", "admin")
@limiter.limit("20 per hour")
def upload_fit():
________________________________________
/api/run_analysis
@csrf.exempt
@research_bp.route("/api/run_analysis", methods=["POST"])
@login_required
@role_required("researcher", "admin")
@limiter.limit("60 per hour")
def run_analysis():
________________________________________
/api/qa/run_pipeline
Plik:
routes/ai_qa_routes.py
@csrf.exempt
@ai_qa_bp.route("/api/qa/run_pipeline", methods=["POST"])
@login_required
@role_required("admin")
@limiter.limit("5 per hour")
def run_pipeline():
________________________________________
/api/performance/run
Plik:
routes/performance_routes.py
@csrf.exempt
@performance_bp.route("/api/performance/run", methods=["POST"])
@login_required
@role_required("admin")
@limiter.limit("10 per hour")
def run_performance():
________________________________________
26.5. Pułapki
Jeśli dostaniesz:
NameError: name 'limiter' is not defined
dodaj w danym pliku:
from security.limiter import limiter
Jeśli dostaniesz:
NameError: name 'csrf' is not defined
dodaj:
from security.csrf import csrf
________________________________________
27. ETAP 19 — logi
Utwórz folder:
logs/
W nim utwórz:
logs/.gitkeep
logs/archived/.gitkeep
Finalna struktura:
logs/
├── .gitkeep
└── archived/
    └── .gitkeep
Logi .log nie powinny iść do repozytorium.
________________________________________
27.1. .gitignore
Dodaj do .gitignore:
logs/*.log
logs/**/*.log

!logs/.gitkeep
!logs/archived/.gitkeep
________________________________________
27.2. Docelowe logi aplikacji
Aplikacja może tworzyć:
logs/app.log
logs/security.log
logs/performance.log
logs/ai.log
logs/uploads.log
logs/auth.log
logs/errors.log
logs/access.log
________________________________________
27.3. Plik services/logger_service.py
Utwórz:
services/logger_service.py
Zawartość:
import logging
import os


os.makedirs(
    "logs",
    exist_ok=True
)


def build_logger(name, filename):

    logger = logging.getLogger(name)
    logger.setLevel(logging.INFO)

    if not logger.handlers:

        formatter = logging.Formatter(
            "%(asctime)s | %(levelname)s | %(message)s"
        )

        file_handler = logging.FileHandler(
            f"logs/{filename}"
        )

        file_handler.setFormatter(formatter)

        logger.addHandler(file_handler)

    return logger


app_logger = build_logger("app_logger", "app.log")
security_logger = build_logger("security_logger", "security.log")
performance_logger = build_logger("performance_logger", "performance.log")
ai_logger = build_logger("ai_logger", "ai.log")
upload_logger = build_logger("upload_logger", "uploads.log")
auth_logger = build_logger("auth_logger", "auth.log")
error_logger = build_logger("error_logger", "errors.log")
access_logger = build_logger("access_logger", "access.log")
________________________________________
28. ETAP 20 — monitoring folder
W starszej wersji tutoriala health_routes.py był w:
monitoring/health_routes.py
Finalnie lepiej trzymać endpoint Flask w:
routes/health_routes.py
Folder:
monitoring/
zostawiamy dla:
Prometheus
Grafana
dashboardów
alertów
konfiguracji monitoringu
Finalna struktura:
routes/
├── health_routes.py
├── research_routes.py
├── performance_routes.py
├── ai_qa_routes.py
└── ...
oraz:
monitoring/
├── prometheus.yml
└── grafana/
    └── dashboards/
________________________________________
29. ETAP 21 — health endpoint
Utwórz plik:
routes/health_routes.py
Finalna wersja:
from flask import Blueprint, jsonify
from datetime import datetime
import time
import os

from database_postgres import db


health_bp = Blueprint(
    "health",
    __name__
)

START_TIME = time.time()


@health_bp.route("/api/health")
def health():

    result = {
        "status": "ok",
        "service": "CoreLabTech",
        "version": "1.0.0",
        "environment": os.getenv(
            "APP_ENV",
            "development"
        ),
        "database": "unknown",
        "database_latency_ms": None,
        "uptime_seconds": round(
            time.time() - START_TIME,
            2
        ),
        "timestamp": datetime.utcnow().isoformat() + "Z"
    }

    con = None
    c = None

    try:

        start = time.time()

        con = db()
        c = con.cursor()

        c.execute("SELECT 1")
        c.fetchone()

        latency_ms = round(
            (time.time() - start) * 1000,
            2
        )

        result["database"] = "ok"
        result["database_latency_ms"] = latency_ms

    except Exception as e:

        result["status"] = "error"
        result["database"] = "error"
        result["error"] = str(e)

        return jsonify(result), 500

    finally:

        if c:
            c.close()

        if con:
            con.close()

    return jsonify(result)
________________________________________
29.1. Rejestracja w app.py
Dodaj import:
from routes.health_routes import health_bp
Dodaj rejestrację blueprintu:
app.register_blueprint(health_bp)
________________________________________
29.2. Test lokalny
http://127.0.0.1:5000/api/health
albo w Dockerze:
http://localhost:5000/api/health
Poprawny wynik:
{
  "database": "ok",
  "environment": "production",
  "service": "CoreLabTech",
  "status": "ok",
  "version": "1.0.0"
}
________________________________________
30. ETAP 22 — Dockerfile
Utwórz w katalogu głównym:
Dockerfile
Uwaga: nazwa ma być dokładnie:
Dockerfile
z dużą literą D, bez rozszerzenia.
________________________________________
30.1. Finalna wersja produkcyjna Dockerfile
FROM python:3.11-bookworm

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

RUN apt-get update && apt-get install -y \
    build-essential \
    gcc \
    curl \
    git \
    ca-certificates \
    nodejs \
    npm \
    default-jdk \
    maven \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

RUN npx playwright install --with-deps chromium

COPY . .

RUN mkdir -p logs
RUN mkdir -p data/uploads/temp
RUN mkdir -p tests/performance/gatling/target

EXPOSE 5000

CMD ["gunicorn", "app:app", "--workers", "3", "--bind", "0.0.0.0:5000", "--timeout", "300"]
________________________________________
30.2. Dlaczego python:3.11-bookworm
W trakcie wdrożenia starsza wersja:
FROM python:3.12-slim
albo:
FROM python:3.11-slim
mogła powodować błąd:
apt-get exit code 100
Dlatego finalnie używamy:
FROM python:3.11-bookworm
________________________________________
30.3. Dlaczego default-jdk
W starszym Dockerfile było:
openjdk-17-jdk
Na obrazie slim/bookworm może to powodować problemy z dostępnością pakietów.
Finalnie:
default-jdk
________________________________________
30.4. Dlaczego tylko Chromium dla Playwright
Zamiast:
RUN npx playwright install --with-deps
używamy:
RUN npx playwright install --with-deps chromium
Bo:
•	build jest szybszy, 
•	mniej waży, 
•	wystarczy do testów Chromium, 
•	zmniejsza ryzyko błędów builda. 
________________________________________
31. ETAP 23 — entrypoint.sh
W finalnym Dockerfile aplikacja startuje bezpośrednio przez CMD, więc entrypoint.sh nie jest konieczny.
Możesz jednak zostawić plik dla przyszłej produkcji.
Utwórz:
entrypoint.sh
Zawartość:
#!/bin/sh

set -e

echo "Starting CoreLabTech..."

gunicorn app:app --workers 3 --bind 0.0.0.0:5000 --timeout 300
Na Windows zwróć uwagę:
W VS Code ustaw LF, nie CRLF.
Jeśli używasz finalnego Dockerfile z CMD ["gunicorn", ...], nie musisz robić:
RUN chmod +x entrypoint.sh
________________________________________
32. ETAP 24 — .dockerignore
Utwórz:
.dockerignore
Finalna zawartość:
__pycache__/
*.pyc
*.pyo
*.pyd

venv/
.venv/

.env
.env.local
.env.production

.git/
.gitignore

node_modules/

test-results/
playwright-report/

logs/*.log
logs/**/*.log

data/uploads/temp/

tests/performance/gatling/target/

backup_before_postgres/
Cel:
- nie kopiować sekretów
- nie kopiować cache
- nie kopiować node_modules
- nie kopiować logów
- zmniejszyć obraz Dockera
________________________________________
33. ETAP 25 — docker-compose.yml
Utwórz:
docker-compose.yml
Finalna wersja:
services:

  web:
    build: .

    container_name: corelabtech_web

    restart: unless-stopped

    ports:
      - "5000:5000"

    env_file:
      - .env

    environment:
      DATABASE_URL: postgresql://corelabtech_user:corelabtech_password@db:5432/corelabtech
      APP_ENV: production
      FLASK_ENV: production

    depends_on:
      - db

    volumes:
      - ./logs:/app/logs
      - ./data:/app/data

    command: >
      gunicorn app:app
      --workers 3
      --bind 0.0.0.0:5000
      --timeout 300

    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5

    networks:
      - corelabtech_network


  db:
    image: postgres:16

    container_name: corelabtech_postgres

    restart: unless-stopped

    environment:
      POSTGRES_DB: corelabtech
      POSTGRES_USER: corelabtech_user
      POSTGRES_PASSWORD: corelabtech_password

    ports:
      - "5432:5432"

    volumes:
      - postgres_data:/var/lib/postgresql/data

    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U corelabtech_user -d corelabtech"]
      interval: 10s
      timeout: 5s
      retries: 5

    networks:
      - corelabtech_network


networks:
  corelabtech_network:
    driver: bridge


volumes:
  postgres_data:
________________________________________
33.1. Ważne — usuń version
Nie dodawaj:
version: "3.9"
Docker Compose v5 pokazuje warning:
the attribute `version` is obsolete
________________________________________
33.2. Ważne — healthcheck PostgreSQL
Nie używaj:
pg_isready -U corelabtech_user
bo PostgreSQL będzie logował:
FATAL: database "corelabtech_user" does not exist
Poprawnie:
pg_isready -U corelabtech_user -d corelabtech
________________________________________
34. ETAP 26 — test Docker lokalnie
34.1. Sprawdź, czy Docker działa
docker info
Jeśli działa, zobaczysz sekcje:
Client:
Server:
Containers:
Images:
________________________________________
34.2. Pierwszy build
W katalogu projektu:
cd D:\corelabtech_final_clean
docker compose build --no-cache
Następnie:
docker compose up -d
Albo jedną komendą:
docker compose up -d --build
________________________________________
34.3. Sprawdź kontenery
docker ps
Poprawnie:
corelabtech_web        Up ... healthy
corelabtech_postgres   Up ... healthy
Jeśli web pokazuje:
health: starting
poczekaj 30–60 sekund i sprawdź ponownie:
docker ps
________________________________________
34.4. Inicjalizacja bazy
W drugim terminalu:
docker compose exec web python init_postgres_db.py
docker compose exec web python seed_postgres_db.py
Poprawny wynik:
PostgreSQL database initialized successfully.

PostgreSQL seed completed
Admin:
  admin@corelabtech.local / Admin123!
Operator:
  operator@corelabtech.local / Operator123!
Researcher:
  researcher@corelabtech.local / Researcher123!
Viewer:
  viewer@corelabtech.local / Viewer123!
________________________________________
34.5. Test health
http://localhost:5000/api/health
Poprawny wynik:
{
  "database": "ok",
  "environment": "production",
  "service": "CoreLabTech",
  "status": "ok"
}
________________________________________
34.6. Test login
http://localhost:5000/login
Dane:
admin@corelabtech.local
Admin123!
________________________________________
34.7. Typowe komendy Docker
Start
docker compose up -d
Start z logami
docker compose up
Stop bez usuwania DB
docker compose down
Rebuild po zmianie Dockerfile albo requirements
docker compose down
docker compose build --no-cache
docker compose up -d
Restart samego web
docker compose restart web
Logi
docker compose logs
docker compose logs web
docker compose logs db
docker compose logs -f
docker compose logs web --tail=80
Wszystkie kontenery, także zatrzymane
docker ps -a
Usuń DB volume
docker compose down -v
Uwaga:
docker compose down -v usuwa dane PostgreSQL z Dockera.
________________________________________
35. ETAP 27 — nginx folder
Utwórz:
nginx/
└── corelabtech.conf
Plik:
nginx/corelabtech.conf
Przykład:
server {
    listen 80;
    server_name app.corelabtech.pl;

    client_max_body_size 100M;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
Na VPS z Dockerem częściej będziesz proxy’ował do:
http://127.0.0.1:5000
bo port 5000 jest wystawiony przez Docker Compose.
________________________________________
36. ETAP 28 — production deployment VPS
Na VPS:
sudo apt update
sudo apt install docker.io docker-compose-plugin nginx certbot python3-certbot-nginx
Sklonuj projekt:
git clone <repo-url>
cd corelabtech_final_clean
Utwórz produkcyjny .env.
Uruchom:
docker compose up -d --build
Inicjalizacja bazy:
docker compose exec web python init_postgres_db.py
docker compose exec web python seed_postgres_db.py
Sprawdź:
docker ps
Sprawdź health:
http://SERVER_IP:5000/api/health
________________________________________
37. ETAP 29 — HTTPS
Na VPS:
sudo certbot --nginx -d app.corelabtech.pl
Po HTTPS ustaw w .env:
SESSION_COOKIE_SECURE=True
APP_ENV=production
FLASK_ENV=production
Restart:
docker compose restart web
Ważne:
SESSION_COOKIE_SECURE=True działa poprawnie dopiero przy HTTPS.
Lokalnie bez HTTPS zostaw False.
________________________________________
38. ETAP 30 — Prometheus config
Utwórz:
monitoring/prometheus.yml
Zawartość:
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "corelabtech"
    static_configs:
      - targets: ["web:5000"]
Endpoint /metrics dodasz przez bibliotekę:
prometheus-flask-exporter
________________________________________
39. ETAP 31 — metrics endpoint
Zainstaluj:
pip install prometheus-flask-exporter
Dodaj do requirements.txt:
prometheus-flask-exporter
W app.py dodaj:
from prometheus_flask_exporter import PrometheusMetrics
Po utworzeniu app:
metrics = PrometheusMetrics(app)

metrics.info(
    "corelabtech_app_info",
    "CoreLabTech application",
    version="1.0.0"
)
Endpoint:
/metrics
powstanie automatycznie.
Test:
http://localhost:5000/metrics
________________________________________
40. ETAP 32 — Grafana
Docelowo w Docker Compose można dodać:
prometheus
grafana
Dashboardy:
API latency
error rate
upload volume
AI analysis usage
PostgreSQL health
CPU/RAM
Docker container health
Gatling performance results
Przykładowe metryki do obserwowania:
request count
request latency
error count
upload count
AI analysis count
DB latency
HTTP 500 rate
________________________________________
41. ETAP 33 — testy po Auth
Po dodaniu Auth stare testy Playwright mogą wymagać logowania.
Dodaj helper:
tests/e2e/helpers/login.ts
Przykład:
import { Page } from '@playwright/test';

export async function loginAsAdmin(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'admin@corelabtech.local');
  await page.fill('input[name="password"]', 'Admin123!');
  await page.click('button[type="submit"]');
}
W testach przed wejściem na /chamber:
await loginAsAdmin(page);
Dla operatora:
export async function loginAsOperator(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'operator@corelabtech.local');
  await page.fill('input[name="password"]', 'Operator123!');
  await page.click('button[type="submit"]');
}
Dla researchera:
export async function loginAsResearcher(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'researcher@corelabtech.local');
  await page.fill('input[name="password"]', 'Researcher123!');
  await page.click('button[type="submit"]');
}
________________________________________
42. ETAP 34 — test ręczny ról
Niezalogowany
Powinien wejść:
/
/about
/technology
/qa
/performance-tests
/ai-testing-lab
/login
/api/health
Nie powinien wejść:
/chamber
/research
/ai-qa-lab
/admin
/debug/db
________________________________________
Viewer
Powinien wejść:
/research
/profile
/api/sessions
/api/user_trends/<own_user_id>
Nie powinien wejść:
/chamber
/upload_csv
/upload_fit
/admin
/debug/db
/api/run_playwright
/api/performance/run
________________________________________
Operator
Powinien wejść:
/chamber
/research
/upload_csv
/upload_fit
/api/save_full_session
/api/during_merge
Nie powinien wejść:
/admin
/debug/db
/api/run_playwright
/api/performance/run
________________________________________
Researcher
Powinien wejść:
/research
/ai-lab
/ai-qa-lab
/api/run_analysis
/api/user_trends
Nie powinien wejść:
/api/delete_user
/api/delete_sessions
/api/run_playwright
/debug/db
/api/performance/run
________________________________________
Admin
Powinien mieć pełny dostęp:
/admin
/debug/db
/ai-qa-lab
/api/run_playwright
/api/performance/run
/api/users
/api/delete_user
/api/delete_sessions
________________________________________
43. ETAP 35 — kolejność wdrożenia bezpieczna
Rób dokładnie tak:
1. Dodaj auth folder
2. Dodaj user_model.py
3. Dodaj decorators.py
4. Dodaj auth_routes.py
5. Dodaj login.html z csrf_token
6. Dodaj unauthorized.html
7. Dodaj profile.html
8. Dodaj LoginManager w app.py
9. Dodaj SECRET_KEY w .env
10. Dodaj csrf.py
11. Dodaj csrf.init_app(app) po app.secret_key
12. Dodaj limiter.py
13. Dodaj limiter.init_app(app)
14. Dodaj security headers
15. Uruchom python app.py lokalnie
16. Wejdź /login
17. Zaloguj się jako admin
18. Dodaj login_required tylko na /chamber
19. Test
20. Dodaj role_required na /admin
21. Test
22. Dodaj ochronę upload_csv/upload_fit
23. Dodaj upload validation
24. Test uploadów
25. Dodaj ochronę save_full_session/during_merge/run_analysis
26. Dodaj ochronę /api/sessions
27. Dodaj ochronę /api/user_trends
28. Dodaj performance admin split
29. Dodaj /api/health
30. Dodaj Dockerfile
31. Dodaj docker-compose.yml
32. Uruchom docker compose build --no-cache
33. Uruchom docker compose up -d
34. Sprawdź docker ps
35. Uruchom init_postgres_db.py
36. Uruchom seed_postgres_db.py
37. Sprawdź /api/health
38. Sprawdź /login
39. Test Playwright po aktualizacji login helper
40. Test Gatling po Auth
41. Dopiero potem VPS
42. Potem Nginx
43. Potem HTTPS
44. Na końcu Prometheus/Grafana
________________________________________
44. Finalny efekt
Po wykonaniu tego tutoriala CoreLabTech będzie mieć:
✅ PostgreSQL
✅ Auth
✅ RBAC
✅ Protected API
✅ Protected AI QA Lab
✅ Protected Performance Admin
✅ Public showcase pages
✅ CSRF
✅ Upload validation
✅ Rate limiting
✅ Security headers
✅ Health endpoint
✅ Docker
✅ Docker healthchecks
✅ PostgreSQL Docker volume
✅ Gunicorn
✅ Nginx-ready config
✅ HTTPS-ready config
✅ Prometheus-ready metrics
✅ Grafana-ready monitoring
✅ Troubleshooting documented
Projekt przechodzi wtedy na poziom:
production-ready AI telemetry & QA platform
________________________________________
45. DODATEK — typowe błędy i naprawy
docker: command not found
Przyczyna:
Docker Desktop nie jest zainstalowany albo terminal nie widzi PATH.
Naprawa:
Uruchom Docker Desktop
Otwórz nowy terminal
docker --version
docker compose version
________________________________________
context deadline exceeded
Przyczyna:
Docker Engine / WSL2 zawiesił się.
Naprawa:
wsl --shutdown
Potem zamknij i uruchom Docker Desktop.
________________________________________
apt-get exit code 100
Naprawa:
FROM python:3.11-bookworm
oraz:
default-jdk
________________________________________
input/output error
Naprawa:
docker compose down
docker builder prune -f
docker system prune -f
Potem:
wsl --shutdown
Jeżeli nadal nie działa:
Docker Desktop → Troubleshoot → Clean / Purge data
________________________________________
No module named flask_cors
Dodaj do requirements.txt:
flask-cors
Potem:
docker compose down
docker compose build --no-cache
docker compose up -d
________________________________________
No module named fitparse
Dodaj:
fitparse
do requirements.txt.
Potem rebuild.
________________________________________
database "corelabtech_user" does not exist
Przyczyna:
pg_isready sprawdza bazę o nazwie użytkownika.
Poprawka w docker-compose.yml:
healthcheck:
  test: ["CMD-SHELL", "pg_isready -U corelabtech_user -d corelabtech"]
________________________________________
yaml: mapping values are not allowed
Przyczyna:
złe wcięcia w docker-compose.yml
taby zamiast spacji
źle wklejony healthcheck
Naprawa:
Podmień cały docker-compose.yml na finalną wersję.
________________________________________
Bad Request — The CSRF token is missing
Przyczyna:
Brakuje csrf_token w login.html.
Naprawa:
<input
    type="hidden"
    name="csrf_token"
    value="{{ csrf_token() }}"
>
I upewnij się, że w app.py:
app.secret_key = ...
csrf.init_app(app)
secret_key jest przed csrf.init_app(app).
________________________________________
Login nie reaguje
Sprawdź logi:
docker compose logs web --tail=80
Sprawdź userów:
docker compose exec db psql -U corelabtech_user -d corelabtech -c "SELECT email, role, password_hash IS NOT NULL AS has_password FROM users;"
Jeśli brakuje userów:
docker compose exec web python seed_postgres_db.py


46. DODATEK — finalny Docker status workflow
Ten rozdział służy do codziennej pracy z Dockerem.
________________________________________
46.1. Sprawdź, czy Docker Engine działa
docker info
Jeżeli Docker działa, zobaczysz m.in.:
Client:
Server:
Containers:
Images:
Jeżeli Docker nie działa, zobaczysz np.:
Cannot connect to the Docker daemon
context deadline exceeded
Naprawa:
wsl --shutdown
Potem uruchom ponownie Docker Desktop.
________________________________________
46.2. Sprawdź, czy CoreLabTech działa
docker ps
Poprawny wynik:
corelabtech_web        Up ... healthy
corelabtech_postgres   Up ... healthy
Jeżeli docker ps jest puste, kontenery nie działają.
Uruchom:
docker compose up -d
________________________________________
46.3. Sprawdź zatrzymane kontenery
docker ps -a
Statusy:
Up          → działa
Exited      → zatrzymany
Restarting  → zapętla restart
unhealthy   → healthcheck nie przechodzi
________________________________________
46.4. Sprawdź logi
Wszystkie:
docker compose logs
Tylko aplikacja:
docker compose logs web
Tylko PostgreSQL:
docker compose logs db
Live:
docker compose logs -f
Ostatnie 80 linii web:
docker compose logs web --tail=80
________________________________________
47. DODATEK — finalna diagnostyka loginu
47.1. Login działa poprawnie, jeśli:
http://localhost:5000/login
po zalogowaniu:
admin@corelabtech.local
Admin123!
przekieruje na:
/chamber
________________________________________
47.2. Jeśli login pokazuje CSRF error
Błąd:
Bad Request
The CSRF token is missing.
Sprawdź w templates/login.html, czy formularz ma:
<input
    type="hidden"
    name="csrf_token"
    value="{{ csrf_token() }}"
>
Sprawdź w app.py, czy kolejność jest taka:
app.secret_key = os.getenv(
    "SECRET_KEY",
    "dev-secret-change-me"
)

csrf.init_app(app)
________________________________________
47.3. Jeśli login nie reaguje
Sprawdź logi:
docker compose logs web --tail=80
Sprawdź, czy userzy istnieją:
docker compose exec db psql -U corelabtech_user -d corelabtech -c "SELECT email, role, password_hash IS NOT NULL AS has_password FROM users;"
Jeśli nie istnieją:
docker compose exec web python seed_postgres_db.py
________________________________________
48. DODATEK — finalny test aplikacji po Dockerze
Po starcie Dockera wykonaj:
docker ps
Powinno być:
corelabtech_web        healthy
corelabtech_postgres   healthy
Następnie sprawdź:
http://localhost:5000/api/health
Poprawnie:
{
  "database": "ok",
  "environment": "production",
  "service": "CoreLabTech",
  "status": "ok"
}
Następnie:
http://localhost:5000/login
Dane:
admin@corelabtech.local
Admin123!
________________________________________
49. DODATEK — finalny test RBAC ręcznie
Niezalogowany
Powinien wejść:
/
/about
/technology
/contact
/qa
/performance-tests
/ai-testing-lab
/login
/api/health
Nie powinien wejść:
/chamber
/research
/ai-qa-lab
/admin
/debug/db
________________________________________
Viewer
Login:
viewer@corelabtech.local
Viewer123!
Powinien wejść:
/research
/profile
/api/sessions
Nie powinien wejść:
/chamber
/admin
/debug/db
/api/run_playwright
/api/performance/run
________________________________________
Operator
Login:
operator@corelabtech.local
Operator123!
Powinien wejść:
/chamber
/research
/upload_csv
/upload_fit
/api/save_full_session
/api/during_merge
Nie powinien wejść:
/admin
/debug/db
/api/run_playwright
/api/performance/run
________________________________________
Researcher
Login:
researcher@corelabtech.local
Researcher123!
Powinien wejść:
/research
/ai-lab
/ai-qa-lab
/api/run_analysis
/api/user_trends
Nie powinien wejść:
/api/delete_user
/api/delete_sessions
/api/run_playwright
/debug/db
/api/performance/run
________________________________________
Admin
Login:
admin@corelabtech.local
Admin123!
Powinien mieć pełny dostęp.
________________________________________
50. DODATEK — finalny test Playwright po Auth
Po dodaniu Auth stare testy E2E muszą się logować.
Utwórz:
tests/e2e/helpers/login.ts
Zawartość:
import { Page } from '@playwright/test';

export async function loginAsAdmin(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'admin@corelabtech.local');
  await page.fill('input[name="password"]', 'Admin123!');
  await page.click('button[type="submit"]');
}

export async function loginAsOperator(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'operator@corelabtech.local');
  await page.fill('input[name="password"]', 'Operator123!');
  await page.click('button[type="submit"]');
}

export async function loginAsResearcher(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'researcher@corelabtech.local');
  await page.fill('input[name="password"]', 'Researcher123!');
  await page.click('button[type="submit"]');
}
W testach:
import { loginAsAdmin } from './helpers/login';

test('admin can access dashboard', async ({ page }) => {
  await loginAsAdmin(page);
  await page.goto('/admin');
});
________________________________________
51. DODATEK — finalny test Gatling po Auth
Jeżeli endpoint Gatling wymaga admina, najprościej:
/api/performance/run
testować z poziomu UI po zalogowaniu adminem.
Jeżeli chcesz testować API bez UI, musisz:
•	najpierw wykonać login, 
•	utrzymać cookie session, 
•	dopiero potem odpalić POST /api/performance/run. 
Na tym etapie najbezpieczniej:
Gatling → testuje API publiczne i health
Playwright → testuje flow z loginem i admin panel
________________________________________
52. DODATEK — finalna checklista przed VPS
Przed VPS sprawdź lokalnie:
✅ docker ps pokazuje web healthy
✅ docker ps pokazuje postgres healthy
✅ /api/health zwraca database ok
✅ /login działa
✅ admin może wejść /admin
✅ researcher może wejść /ai-qa-lab
✅ operator może wejść /chamber
✅ publiczny user widzi /performance-tests bez przycisków
✅ admin widzi przyciski performance
✅ upload CSV działa
✅ upload FIT działa
✅ AI analysis działa
✅ Playwright działa po login helper
✅ Gatling działa albo jest ograniczony do admin flow
________________________________________
53. DODATEK — finalna checklista po deployu na VPS
Po deployu:
docker compose up -d --build
docker compose exec web python init_postgres_db.py
docker compose exec web python seed_postgres_db.py
docker ps
Sprawdź:
http://SERVER_IP:5000/api/health
Po Nginx:
http://app.corelabtech.pl/api/health
Po HTTPS:
https://app.corelabtech.pl/api/health
________________________________________
54. DODATEK — finalna checklista HTTPS
Po certbocie:
sudo certbot --nginx -d app.corelabtech.pl
W .env ustaw:
SESSION_COOKIE_SECURE=True
APP_ENV=production
FLASK_ENV=production
Restart:
docker compose restart web
Test:
https://app.corelabtech.pl/login
________________________________________
55. DODATEK — finalna checklista monitoringu
Sprawdź:
/api/health
/metrics
Docelowo Prometheus zbiera:
request count
request latency
error count
DB latency
upload count
AI analysis count
performance endpoint count
Grafana dashboardy:
API latency
error rate
upload volume
AI usage
PostgreSQL health
Docker container health
CPU/RAM
disk usage
________________________________________
56. FINALNY STATUS PRODUKCYJNY
Po wykonaniu całego tutoriala CoreLabTech ma:
✅ PostgreSQL
✅ Docker PostgreSQL volume
✅ Flask
✅ Gunicorn
✅ Auth
✅ Login/logout
✅ RBAC
✅ CSRF
✅ Protected API
✅ Protected AI QA Lab
✅ Protected Performance Admin
✅ Public showcase pages
✅ Upload validation
✅ Rate limiting
✅ Security headers
✅ Health endpoint
✅ Docker healthchecks
✅ Nginx-ready config
✅ HTTPS-ready config
✅ Prometheus-ready metrics
✅ Grafana-ready monitoring
✅ Playwright after-auth flow
✅ Gatling performance framework
✅ Troubleshooting documented
Projekt przechodzi wtedy na poziom:
production-ready AI telemetry & QA platform

57. Nginx-ready config
Plik:
nginx/corelabtech.conf
server {
    listen 80;
    server_name app.corelabtech.pl;

    client_max_body_size 100M;

    location / {
        proxy_pass http://127.0.0.1:5000;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
    }
}
Test Nginx:
sudo nginx -t
sudo systemctl reload nginx
58. HTTPS-ready config
Instalacja:
sudo apt install certbot python3-certbot-nginx
Certyfikat:
sudo certbot --nginx -d app.corelabtech.pl
Po HTTPS w .env:
SESSION_COOKIE_SECURE=True
APP_ENV=production
FLASK_ENV=production
Restart:
docker compose restart web
Test:
https://app.corelabtech.pl/api/health
https://app.corelabtech.pl/login
59. Prometheus-ready metrics
W requirements.txt:
prometheus-flask-exporter
W app.py:
from prometheus_flask_exporter import PrometheusMetrics

metrics = PrometheusMetrics(app)

metrics.info(
    "corelabtech_app_info",
    "CoreLabTech application",
    version="1.0.0"
)
Test:
http://localhost:5000/metrics
monitoring/prometheus.yml:
global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "corelabtech"
    static_configs:
      - targets: ["web:5000"]
60. Grafana-ready monitoring
Docelowe dashboardy:
API latency
error rate
request volume
upload count
AI analysis count
DB latency
Docker container health
CPU/RAM
disk usage
Najpierw monitoruj:
/api/health
/metrics
PostgreSQL healthcheck
Docker health status
61. Playwright after-auth flow
Plik:
tests/e2e/helpers/login.ts
import { Page } from '@playwright/test';

export async function loginAsAdmin(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'admin@corelabtech.local');
  await page.fill('input[name="password"]', 'Admin123!');
  await page.click('button[type="submit"]');
}

export async function loginAsOperator(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'operator@corelabtech.local');
  await page.fill('input[name="password"]', 'Operator123!');
  await page.click('button[type="submit"]');
}

export async function loginAsResearcher(page: Page) {
  await page.goto('/login');
  await page.fill('input[name="email"]', 'researcher@corelabtech.local');
  await page.fill('input[name="password"]', 'Researcher123!');
  await page.click('button[type="submit"]');
}
Przykład testu:
import { test, expect } from '@playwright/test';
import { loginAsAdmin } from './helpers/login';

test('admin can access AI QA Lab', async ({ page }) => {
  await loginAsAdmin(page);
  await page.goto('/ai-qa-lab');
  await expect(page).toHaveURL(/ai-qa-lab/);
});
62. Gatling performance framework
Zasada po Auth:
Gatling testuje publiczne endpointy i health bez logowania.
Admin-only performance run testujesz przez Playwright albo przez sesję/cookie.
Bezpieczne endpointy dla Gatling:
GET /api/health
GET /performance-tests
GET /qa
Admin endpointy:
POST /api/performance/run
wymagają logowania i roli admin.
63. Troubleshooting documented
Najważniejsze błędy:
docker: command not found
context deadline exceeded
apt-get exit code 100
input/output error
No module named flask_cors
No module named fitparse
database "corelabtech_user" does not exist
yaml mapping values are not allowed
The CSRF token is missing
Login nie reaguje
Diagnostyka:
docker info
docker ps
docker ps -a
docker compose logs web --tail=80
docker compose logs db --tail=80
docker compose exec db psql -U corelabtech_user -d corelabtech -c "SELECT email, role, password_hash IS NOT NULL AS has_passwo

64. FINALNY DEPLOY — od domeny do produkcji
64.1. Co kupić
Potrzebujesz:
1. domena
2. VPS Linux
NIE zwykły hosting WWW.
CoreLabTech wymaga:
Docker
PostgreSQL
Gunicorn
Nginx
Playwright
czyli potrzebujesz VPS.
________________________________________
64.2. Polecany VPS
Na start:
Hetzner
Najlepszy cena/jakość.
Minimum:
2 vCPU
4 GB RAM
40–80 GB SSD
Ubuntu 24.04
________________________________________
Alternatywy
OVH VPS
Contabo
DigitalOcean
Linode
________________________________________
64.3. Zakup domeny
Przykłady:
corelabtech.ai
corelabtech.pl
corelabtech.io
Kup np. w:
OVH
Cloudflare
Namecheap
________________________________________
64.4. Konfiguracja DNS
Po zakupie domeny dodaj rekord:
A record
Type: A
Host: @
Value: IP_SERWERA
oraz:
Type: A
Host: app
Value: IP_SERWERA
Po kilku minutach:
app.corelabtech.pl
będzie wskazywać VPS.
________________________________________
65. ETAP — pierwszy login na VPS
Po zakupie VPS dostaniesz:
IP
login root
hasło
Połącz się:
Windows PowerShell
ssh root@IP_SERWERA
________________________________________
66. ETAP — przygotowanie Ubuntu
Aktualizacja
apt update && apt upgrade -y
________________________________________
Instalacja Dockera
apt install docker.io docker-compose-plugin -y
________________________________________
Start Dockera
systemctl enable docker
systemctl start docker
Test:
docker info
________________________________________
67. ETAP — instalacja Git
apt install git -y
________________________________________
68. ETAP — pobranie projektu
GitHub
git clone https://github.com/TWOJE_REPO/corelabtech.git
lub prywatne repo.
Potem:
cd corelabtech
________________________________________
69. ETAP — produkcyjny .env
Na VPS:
nano .env
FINALNA produkcja:
SECRET_KEY=SUPER_SECRET_RANDOM_KEY

DATABASE_URL=postgresql://corelabtech_user:corelabtech_password@db:5432/corelabtech

APP_ENV=production
FLASK_ENV=production

SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SECURE=True
REMEMBER_COOKIE_HTTPONLY=True
________________________________________
70. ETAP — pierwszy build na VPS
docker compose build --no-cache
Potem:
docker compose up -d
________________________________________
71. ETAP — sprawdzenie kontenerów
docker ps
Poprawnie:
corelabtech_web        healthy
corelabtech_postgres   healthy
________________________________________
72. ETAP — inicjalizacja PostgreSQL
docker compose exec web python init_postgres_db.py
Potem:
docker compose exec web python seed_postgres_db.py
________________________________________
73. ETAP — test aplikacji po IP
Wejdź:
http://IP_SERWERA:5000/api/health
Jeśli działa:
{
  "status":"ok"
}
to aplikacja działa.
________________________________________
74. ETAP — instalacja Nginx
apt install nginx -y
________________________________________
75. ETAP — konfiguracja reverse proxy
Plik:
nano /etc/nginx/sites-available/corelabtech
Zawartość:
server {

    listen 80;

    server_name app.corelabtech.pl;

    client_max_body_size 100M;

    location / {

        proxy_pass http://127.0.0.1:5000;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
    }
}
________________________________________
76. ETAP — aktywacja Nginx
ln -s /etc/nginx/sites-available/corelabtech /etc/nginx/sites-enabled/
Test:
nginx -t
Reload:
systemctl reload nginx
________________________________________
77. ETAP — test domeny
Wejdź:
http://app.corelabtech.pl
Powinieneś zobaczyć CoreLabTech.
________________________________________
78. ETAP — HTTPS
Instalacja:
apt install certbot python3-certbot-nginx -y
Certyfikat:
certbot --nginx -d app.corelabtech.pl
Certbot:
•	skonfiguruje HTTPS, 
•	zrobi redirect HTTP → HTTPS. 
________________________________________
79. ETAP — restart po HTTPS
Po HTTPS:
docker compose restart web
________________________________________
80. ETAP — test HTTPS
Sprawdź:
https://app.corelabtech.pl
https://app.corelabtech.pl/login
https://app.corelabtech.pl/api/health
________________________________________
81. ETAP — firewall
Ubuntu:
ufw allow OpenSSH
ufw allow 80
ufw allow 443
ufw enable
________________________________________
82. ETAP — nie wystawiaj PostgreSQL publicznie
W produkcji usuń:
ports:
  - "5432:5432"
z PostgreSQL w docker-compose.yml.
DB ma być dostępna tylko wewnątrz Dockera.
________________________________________
83. ETAP — backup PostgreSQL
Backup:
docker compose exec db pg_dump -U corelabtech_user corelabtech > backup.sql
Restore:
cat backup.sql | docker compose exec -T db psql -U corelabtech_user corelabtech
________________________________________
84. ETAP — aktualizacja aplikacji
Po zmianach kodu:
git pull
docker compose down
docker compose build --no-cache
docker compose up -d
________________________________________
85. ETAP — restart po crashu
Docker Compose ma:
restart: unless-stopped
więc po restarcie VPS aplikacja wstanie automatycznie.
________________________________________
86. ETAP — monitoring produkcyjny
Codzienne komendy:
Status
docker ps
Logi
docker compose logs web --tail=80
Health
https://app.corelabtech.pl/api/health
________________________________________
87. ETAP — Prometheus/Grafana produkcyjnie
Docelowo dodasz:
Prometheus
Grafana
Node Exporter
cAdvisor
Monitoring:
CPU
RAM
disk
Docker
PostgreSQL
API latency
AI usage
upload rate
error rate
________________________________________
88. ETAP — CI/CD (później)
Docelowo:
GitHub Actions
Pipeline:
push → tests → docker build → deploy VPS
________________________________________
89. ETAP — finalna architektura produkcyjna
Internet
    ↓
Cloudflare/DNS
    ↓
Nginx + HTTPS
    ↓
Docker Compose
    ↓
Gunicorn Flask
    ↓
PostgreSQL
________________________________________
90. FINALNY STATUS
Po wszystkim masz:
✅ Production VPS
✅ Docker
✅ Gunicorn
✅ PostgreSQL
✅ HTTPS
✅ Nginx
✅ RBAC
✅ CSRF
✅ Upload validation
✅ Monitoring ready
✅ Prometheus ready
✅ Grafana ready
✅ AI QA framework
✅ Playwright
✅ Gatling
✅ Docker healthchecks
✅ Secure production deployment
To już jest poziom:
production-grade AI telemetry platform
91. HTTPS — pełna konfiguracja produkcyjna
91.1. Dlaczego HTTPS jest krytyczny
Bez HTTPS:
- cookies sesji mogą być przechwycone
- login nie jest bezpieczny
- SESSION_COOKIE_SECURE=True nie działa poprawnie
- przeglądarka pokaże "Not Secure"
- CSRF/session security jest słabsze
Dla CoreLabTech HTTPS jest obowiązkowy.
________________________________________
91.2. Finalna architektura HTTPS
Docelowy przepływ:
Internet
    ↓
HTTPS (Let's Encrypt)
    ↓
Nginx reverse proxy
    ↓
Gunicorn Flask
    ↓
Docker Compose
    ↓
PostgreSQL
HTTPS kończy się na:
Nginx
Flask/Gunicorn działa wewnętrznie po HTTP.
________________________________________
91.3. DNS przed HTTPS
HTTPS NIE zadziała, dopóki domena nie wskazuje poprawnie na VPS.
Sprawdź:
ping app.corelabtech.pl
Powinieneś zobaczyć IP VPS.
________________________________________
91.4. Instalacja Nginx
Na VPS:
apt update
apt install nginx -y
Start:
systemctl enable nginx
systemctl start nginx
Status:
systemctl status nginx
Powinno być:
active (running)
________________________________________
91.5. Konfiguracja reverse proxy
Plik:
nano /etc/nginx/sites-available/corelabtech
FINALNA konfiguracja HTTP przed certyfikatem:
server {

    listen 80;

    server_name app.corelabtech.pl;

    client_max_body_size 100M;

    location / {

        proxy_pass http://127.0.0.1:5000;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 300;

        proxy_send_timeout 300;

        proxy_read_timeout 300;
    }
}
________________________________________
91.6. Aktywacja konfiguracji
ln -s /etc/nginx/sites-available/corelabtech /etc/nginx/sites-enabled/
Test:
nginx -t
Poprawny wynik:
syntax is ok
test is successful
Reload:
systemctl reload nginx
________________________________________
91.7. Test HTTP przed HTTPS
Sprawdź:
http://app.corelabtech.pl
Powinno działać jeszcze bez certyfikatu.
Jeśli nie działa:
sprawdź Docker
docker ps
sprawdź Flask/Gunicorn
docker compose logs web --tail=80
sprawdź Nginx
systemctl status nginx
________________________________________
91.8. Instalacja Certbot
Na Ubuntu:
apt install certbot python3-certbot-nginx -y
________________________________________
91.9. Generowanie certyfikatu HTTPS
Uruchom:
certbot --nginx -d app.corelabtech.pl
Certbot:
•	pobierze certyfikat Let's Encrypt, 
•	automatycznie zmodyfikuje Nginx, 
•	doda redirect HTTP → HTTPS. 
________________________________________
91.10. Finalna konfiguracja Nginx po HTTPS
Po certbocie Nginx zwykle wygląda tak:
server {

    server_name app.corelabtech.pl;

    client_max_body_size 100M;

    location / {

        proxy_pass http://127.0.0.1:5000;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 300;

        proxy_send_timeout 300;

        proxy_read_timeout 300;
    }

    listen 443 ssl;

    ssl_certificate /etc/letsencrypt/live/app.corelabtech.pl/fullchain.pem;

    ssl_certificate_key /etc/letsencrypt/live/app.corelabtech.pl/privkey.pem;
}


server {

    listen 80;

    server_name app.corelabtech.pl;

    return 301 https://$host$request_uri;
}
________________________________________
91.11. Test HTTPS
Sprawdź:
https://app.corelabtech.pl
https://app.corelabtech.pl/login
https://app.corelabtech.pl/api/health
Powinieneś zobaczyć:
•	kłódkę HTTPS, 
•	brak ostrzeżeń, 
•	redirect z HTTP → HTTPS. 
________________________________________
91.12. SESSION_COOKIE_SECURE=True
Po HTTPS MUSISZ zmienić .env.
Przed HTTPS
SESSION_COOKIE_SECURE=False
Po HTTPS
SESSION_COOKIE_SECURE=True
________________________________________
91.13. Restart aplikacji po zmianie .env
docker compose restart web
________________________________________
91.14. Dlaczego SESSION_COOKIE_SECURE=True jest ważne
To wymusza:
cookies sesji tylko przez HTTPS
Bez tego:
•	sesja może być wysłana po HTTP, 
•	bezpieczeństwo loginu jest słabsze. 
________________________________________
91.15. Typowy błąd po HTTPS
Jeśli po włączeniu:
SESSION_COOKIE_SECURE=True
login przestaje działać lokalnie:
to normalne.
HTTPS nie działa na:
http://localhost:5000
Dlatego:
lokalnie
SESSION_COOKIE_SECURE=False
produkcja HTTPS
SESSION_COOKIE_SECURE=True
________________________________________
91.16. Automatyczne odnawianie certyfikatu
Let's Encrypt odnawia certyfikaty automatycznie.
Test:
certbot renew --dry-run
________________________________________
91.17. Firewall dla HTTPS
Ubuntu UFW:
ufw allow OpenSSH
ufw allow 80
ufw allow 443
ufw enable
________________________________________
91.18. Nie wystawiaj PostgreSQL publicznie
W produkcji usuń:
ports:
  - "5432:5432"
z PostgreSQL w docker-compose.yml.
DB ma być tylko:
•	wewnątrz Dockera, 
•	niedostępna z Internetu. 
________________________________________
91.19. Security headers + HTTPS
W security/headers.py masz już:
response.headers["X-Frame-Options"] = "SAMEORIGIN"
response.headers["X-Content-Type-Options"] = "nosniff"
Po HTTPS warto dodać:
response.headers["Strict-Transport-Security"] = (
    "max-age=31536000; includeSubDomains"
)
FINALNIE:
response.headers["Strict-Transport-Security"] = (
    "max-age=31536000; includeSubDomains"
)
________________________________________
91.20. Test security headers
Sprawdź:
curl -I https://app.corelabtech.pl
Powinieneś zobaczyć:
Strict-Transport-Security
X-Frame-Options
X-Content-Type-Options
Content-Security-Policy
________________________________________
91.21. Finalna architektura HTTPS
Browser
    ↓ HTTPS
Nginx
    ↓ HTTP internal
Gunicorn Flask
    ↓
Docker Network
    ↓
PostgreSQL
________________________________________
91.22. Finalna checklista HTTPS
✅ domena działa
✅ DNS wskazuje VPS
✅ nginx działa
✅ docker web działa
✅ certbot działa
✅ HTTPS działa
✅ redirect HTTP → HTTPS działa
✅ SESSION_COOKIE_SECURE=True
✅ login działa po HTTPS
✅ /api/health działa po HTTPS
✅ security headers działają
✅ certbot renew działa


Najmniej dokładnie omówione zostały jeszcze te obszary:
1.	Backup i restore PostgreSQL
Jak robić backupy automatyczne, gdzie je trzymać, jak przywrócić bazę po awarii. 
2.	Aktualizacja produkcji bez kasowania danych
Dokładny workflow: git pull, rebuild, restart, migracje, rollback. 
3.	Firewall i twarde zabezpieczenie VPS
UFW, SSH keys zamiast hasła, blokada root login, Fail2Ban. 
4.	Usunięcie publicznego portu PostgreSQL w produkcji
Lokalnie 5432:5432 jest OK, produkcyjnie DB nie powinna być wystawiona. 
5.	Sekrety produkcyjne
Silny SECRET_KEY, hasła DB, .env, brak sekretów w repo, rotacja haseł. 
6.	Migracje bazy danych
Teraz masz init_postgres_db.py, ale produkcyjnie warto opisać Alembic/Flask-Migrate. 
7.	CI/CD
GitHub Actions: testy, build Dockera, deploy na VPS. 
8.	Rollback po nieudanym deployu
Co zrobić, gdy nowa wersja padnie. 
9.	Monitoring alertów
Nie tylko Grafana, ale alerty: DB down, API 500, disk >80%, kontener unhealthy. 
10.	Log rotation
Żeby logs/*.log nie zapełniły dysku. 
11.	Production Docker Compose z Redis/Prometheus/Grafana
Obecnie masz podstawowy web+db. Brakuje pełnego compose produkcyjnego z monitoringiem. 
12.	Procedura pierwszego uruchomienia po zakupie domeny/VPS
Omówiliśmy, ale można jeszcze rozpisać jako checklistę dzień-0/dzień-1. 
Najważniejsze do dopisania teraz: backup/restore, firewall/SSH hardening, production docker-compose, CI/CD, rollback i log rotation.
opisz dokładnie te 12 punktów jako kontynuacja tutoriala
92. BACKUP I RESTORE PostgreSQL
92.1. Dlaczego backup jest krytyczny
Bez backupów możesz stracić:
sessions
AI results
FIT/CSV telemetry
users
research data
W produkcji backup musi być:
•	automatyczny, 
•	regularny, 
•	testowany restore’em. 
________________________________________
92.2. Ręczny backup PostgreSQL
Na VPS:
docker compose exec db pg_dump -U corelabtech_user corelabtech > backup.sql
Powstanie:
backup.sql
________________________________________
92.3. Restore PostgreSQL
cat backup.sql | docker compose exec -T db psql -U corelabtech_user corelabtech
________________________________________
92.4. Backup z timestampem
Lepsza wersja:
mkdir -p backups

docker compose exec db pg_dump -U corelabtech_user corelabtech > backups/corelabtech_$(date +%Y%m%d_%H%M%S).sql
________________________________________
92.5. Automatyczny backup cron
Edytuj cron:
crontab -e
Codziennie 2:00:
0 2 * * * cd /root/corelabtech && docker compose exec -T db pg_dump -U corelabtech_user corelabtech > backups/corelabtech_$(date +\%Y\%m\%d_\%H\%M\%S).sql
________________________________________
92.6. Backup poza VPS
Backup tylko na VPS nie wystarczy.
Docelowo:
•	S3, 
•	Google Drive, 
•	inny serwer, 
•	Backblaze. 
________________________________________
92.7. Test restore
Backup bez test restore jest bezużyteczny.
Regularnie:
docker compose down -v
docker compose up -d
restore backup
________________________________________
93. AKTUALIZACJA PRODUKCJI BEZ UTRATY DANYCH
93.1. Finalny workflow deployu
Na VPS:
cd /root/corelabtech

git pull

docker compose down

docker compose build --no-cache

docker compose up -d
________________________________________
93.2. Dlaczego down nie usuwa danych
Bo PostgreSQL używa volume:
volumes:
  - postgres_data:/var/lib/postgresql/data
Dane znikają dopiero po:
docker compose down -v
________________________________________
93.3. Kiedy używać down -v
PRAWIE NIGDY na produkcji.
To:
•	usuwa DB, 
•	usuwa wszystkie dane. 
________________________________________
93.4. Safe production deploy
Kolejność:
backup DB
git pull
build
up -d
healthcheck
test login
test API
________________________________________
94. FIREWALL I HARDENING VPS
94.1. Instalacja UFW
apt install ufw -y
________________________________________
94.2. Otwarcie portów
ufw allow OpenSSH
ufw allow 80
ufw allow 443
________________________________________
94.3. Włączenie firewalla
ufw enable
Status:
ufw status
________________________________________
94.4. SSH keys zamiast hasła
Na lokalnym komputerze:
ssh-keygen
Wyślij klucz:
ssh-copy-id root@IP_SERWERA
________________________________________
94.5. Wyłączenie logowania hasłem
Edytuj:
nano /etc/ssh/sshd_config
Zmodyfikuj:
PasswordAuthentication no
PermitRootLogin prohibit-password
Restart SSH:
systemctl restart ssh
________________________________________
94.6. Fail2Ban
Instalacja:
apt install fail2ban -y
Start:
systemctl enable fail2ban
systemctl start fail2ban
________________________________________
95. UKRYCIE PostgreSQL
95.1. Produkcja
Usuń:
ports:
  - "5432:5432"
z PostgreSQL.
________________________________________
95.2. Dlaczego
DB nie może być publicznie dostępna.
Ma działać tylko:
Docker internal network
________________________________________
95.3. Lokalnie
Możesz zostawić:
5432:5432
dla pgAdmin.
________________________________________
96. SEKRETY PRODUKCYJNE
96.1. SECRET_KEY
Nigdy:
SECRET_KEY=test
Generuj:
python -c "import secrets; print(secrets.token_hex(32))"
________________________________________
96.2. .env
Nigdy nie commituj:
.env
Do repo.
________________________________________
96.3. .env.example
Repo powinno mieć:
SECRET_KEY=CHANGE_ME
DATABASE_URL=CHANGE_ME
________________________________________
96.4. GitHub secrets
Dla CI/CD używaj:
•	GitHub Secrets, 
•	nie plaintext. 
________________________________________
97. MIGRACJE BAZY DANYCH
97.1. Problem obecnego podejścia
init_postgres_db.py jest OK na start, ale:
•	nie śledzi zmian, 
•	nie wersjonuje DB, 
•	nie ma rollback. 
________________________________________
97.2. Produkcyjnie
Docelowo:
Flask-Migrate
Alembic
________________________________________
97.3. Instalacja
pip install flask-migrate
________________________________________
97.4. Workflow
Init
flask db init
Migration
flask db migrate -m "add ai_results"
Upgrade
flask db upgrade
________________________________________
98. CI/CD
98.1. Cel
Automatyzacja:
push → tests → build → deploy
________________________________________
98.2. GitHub Actions
Folder:
.github/workflows/
________________________________________
98.3. Pipeline
name: CoreLabTech CI

on:
  push:
    branches:
      - main

jobs:

  test-and-build:

    runs-on: ubuntu-latest

    steps:

      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Run tests
        run: pytest

      - name: Docker build
        run: docker compose build
________________________________________
98.4. Deploy automation
Później:
GitHub Actions
→ SSH VPS
→ git pull
→ docker compose build
→ docker compose up -d
________________________________________
99. ROLLBACK
99.1. Problem
Nowy deploy może:
•	zepsuć login, 
•	zepsuć DB, 
•	crashować app. 
________________________________________
99.2. Minimalny rollback
git log
Znajdź wcześniejszy commit:
git checkout COMMIT_ID
Potem:
docker compose down
docker compose build --no-cache
docker compose up -d
________________________________________
99.3. Backup przed deploy
ZAWSZE:
backup DB
przed:
git pull
________________________________________
100. ALERTING
100.1. Monitoring ≠ alerting
Grafana dashboard nie wystarczy.
Musisz wiedzieć kiedy:
•	DB padła, 
•	API zwraca 500, 
•	dysk pełny, 
•	RAM pełny. 
________________________________________
100.2. Alerty
Docelowo:
•	Grafana Alerts, 
•	Discord webhook, 
•	Slack, 
•	email. 
________________________________________
100.3. Co monitorować
HTTP 500 rate
DB latency
Docker unhealthy
CPU
RAM
disk usage
upload failure rate
AI error rate
________________________________________
101. LOG ROTATION
101.1. Problem
logs/*.log mogą zapełnić dysk.
________________________________________
101.2. Linux logrotate
Plik:
nano /etc/logrotate.d/corelabtech
________________________________________
101.3. Konfiguracja
/root/corelabtech/logs/*.log {

    daily
    rotate 14

    compress

    missingok
    notifempty

    copytruncate
}
________________________________________
101.4. Efekt
app.log
app.log.1
app.log.2.gz
________________________________________
102. PEŁNY PRODUCTION docker-compose
102.1. Docelowo
Nie tylko:
web + db
ale:
web
db
redis
prometheus
grafana
________________________________________
102.2. Redis
Przyszłościowo:
•	rate limiting, 
•	Celery, 
•	cache, 
•	queue. 
________________________________________
102.3. Prometheus
Metrics scraping.
________________________________________
102.4. Grafana
Dashboardy i alerty.
________________________________________
103. PROCEDURA „DZIEŃ 0”
Po zakupie VPS:
1. SSH login
2. update Ubuntu
3. install Docker
4. install Git
5. clone repo
6. create .env
7. docker compose build
8. docker compose up -d
9. init DB
10. seed DB
11. test health
12. install nginx
13. configure nginx
14. configure DNS
15. certbot HTTPS
16. firewall
17. monitoring
18. backups
________________________________________
104. PROCEDURA „DZIEŃ 1”
Codzienna administracja:
docker ps
docker compose logs web --tail=80
/api/health
backup DB
git pull
deploy
monitoring
________________________________________
105. FINALNY STATUS ENTERPRISE
Po tych etapach CoreLabTech ma:
✅ Docker production
✅ HTTPS
✅ RBAC
✅ CSRF
✅ secure cookies
✅ PostgreSQL
✅ backups
✅ restore
✅ firewall
✅ Fail2Ban
✅ monitoring
✅ alerting
✅ Prometheus
✅ Grafana
✅ CI/CD ready
✅ rollback strategy
✅ log rotation
✅ production hardening
To już architektura:
enterprise-grade AI telemetry platform





============================================================================
KONIEC ARCHIWUM ŹRÓDŁOWEGO — PLIK 2: Wklejony tekst (2)(3).txt
============================================================================

============================================================================
ARCHIWUM ŹRÓDŁOWE — PLIK 3: Wklejony tekst (3).txt
============================================================================

CORELABTECH — FINALNY TUTORIAL WDROŻENIA PRODUKCYJNEGO + DEBUG DOCKER / PLAYWRIGHT / GATLING
VPS Linux M / Docker / Flask / PostgreSQL / Playwright / Gatling / AI QA

============================================================
0. CEL DOKUMENTU
============================================================

Ten dokument jest finalną, scaloną wersją tutoriala CoreLabTech.

Łączy:
1. produkcyjne wdrożenie na VPS,
2. Docker Compose,
3. PostgreSQL,
4. Auth + RBAC,
5. Security hardening,
6. Playwright E2E,
7. Gatling performance tests,
8. AI QA Lab,
9. raporty HTML,
10. realne problemy debugowania, które pojawiły się podczas pracy.

Dokument zakłada, że projekt ma już działające podstawowe moduły:

✅ Docker działa lokalnie
✅ PostgreSQL działa
✅ Flask działa
✅ login działa
✅ role działają
✅ /api/health działa
✅ Docker Compose działa
✅ CSV/FIT upload działa
✅ AI analysis działa
✅ /api/sessions działa
✅ /api/user_trends działa
✅ Playwright działa po poprawkach auth/network
✅ Gatling działa po poprawkach redirect/performance endpoints

Konta lokalne:

admin@corelabtech.local / Admin123!
operator@corelabtech.local / Operator123!
researcher@corelabtech.local / Researcher123!
viewer@corelabtech.local / Viewer123!

UWAGA:
Na produkcji hasła demo muszą zostać zmienione albo konta demo usunięte.

============================================================
1. FINALNA ARCHITEKTURA PRODUKCYJNA
============================================================

Docelowy przepływ:

Internet
    ↓
Domena home.pl
    ↓
Nginx HTTPS
    ↓
Docker Compose
    ↓
Gunicorn Flask
    ↓
PostgreSQL
    ↓
Monitoring / Logs / Reports

Docelowo aplikacja działa jako:

production-grade AI telemetry & QA research platform

Główne moduły:
- HBOT session workflow,
- CSV/FIT upload,
- telemetry merge engine,
- AI analysis,
- AI QA Lab,
- Software QA Lab,
- Playwright E2E reports,
- Gatling performance reports,
- Research dashboard,
- RBAC,
- monitoring,
- Docker deployment.

============================================================
2. FINALNY MODEL DOSTĘPU
============================================================

Publiczne — bez logowania:
/
/about
/technology
/contact
/qa
/performance-tests
/ai-testing-lab

Ważne:
Na /performance-tests publicznie pokazuje się opis architektury.
Przyciski Run Performance są widoczne tylko dla admina.

Po logowaniu:
/chamber
/research
/ai_lab lub /ai-lab
/profile
/logout

Researcher + Admin:
/ai-qa-lab
/api/run_analysis
/api/user_trends/<user_id>
/api/ai_qa/*
/report lub generator raportu research

Admin only:
/admin
/debug/db
/api/run_playwright
/api/performance/run
/api/performance/history
/api/performance/latest
/api/users
/api/delete_user
/api/delete_sessions
/admin/playwright-report/*
/admin/gatling-report/*

============================================================
3. ROLE RBAC
============================================================

viewer
    → podgląd własnych sesji

operator
    → tworzenie sesji HBOT
    → upload CSV/FIT
    → merge
    → save session

researcher
    → AI analysis
    → trendy użytkownika
    → eksporty
    → AI QA Lab
    → research dashboard

admin
    → wszystko
    → QA
    → Playwright
    → Gatling
    → DB debug
    → users
    → monitoring
    → reports

============================================================
4. STRUKTURA PROJEKTU
============================================================

corelabtech_final_clean/
│
├── app.py
├── database_postgres.py
├── .env
├── .env.example
├── requirements.txt
│
├── auth/
│   ├── __init__.py
│   ├── auth_routes.py
│   ├── decorators.py
│   └── user_model.py
│
├── security/
│   ├── __init__.py
│   ├── limiter.py
│   ├── csrf.py
│   ├── headers.py
│   └── upload_validation.py
│
├── monitoring/
│   ├── __init__.py
│   └── health_routes.py
│
├── templates/
│   ├── layout.html
│   ├── login.html
│   ├── unauthorized.html
│   ├── profile.html
│   ├── research_dashboard.html
│   ├── ai_qa_lab.html
│   ├── performance_tests.html
│   └── ai_testing_lab_public.html
│
├── routes/
│   ├── research_routes.py
│   ├── performance_routes.py
│   ├── ai_qa_routes.py
│   ├── qa_routes.py
│   └── ...
│
├── static/
│   ├── css/
│   └── js/
│
├── logs/
│   ├── app.log
│   ├── security.log
│   └── performance.log
│
├── Dockerfile
├── docker-compose.yml
├── .dockerignore
├── entrypoint.sh
│
├── nginx/
│   └── corelabtech.conf
│
├── tests/
│   ├── e2e/
│   └── performance/
│       └── gatling/
│
└── monitoring/
    ├── prometheus.yml
    └── grafana/
        └── dashboards/

============================================================
5. AUTH + RBAC — PAKIETY
============================================================

Instalacja:

pip install flask-login flask-wtf flask-limiter email-validator

requirements.txt:

flask-login
flask-wtf
flask-limiter
email-validator

============================================================
6. TABELA USERS
============================================================

Sprawdzenie:

SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name='users'
ORDER BY ordinal_position;

Tabela users powinna mieć minimum:

id
user_id
email
password_hash
role
is_active

Jeśli brakuje pól:

ALTER TABLE users ADD COLUMN IF NOT EXISTS email VARCHAR(255) UNIQUE;
ALTER TABLE users ADD COLUMN IF NOT EXISTS password_hash TEXT;
ALTER TABLE users ADD COLUMN IF NOT EXISTS role VARCHAR(50) DEFAULT 'viewer';
ALTER TABLE users ADD COLUMN IF NOT EXISTS is_active BOOLEAN DEFAULT TRUE;

============================================================
7. AUTH FOLDER
============================================================

Utwórz:

auth/
├── __init__.py
├── user_model.py
├── decorators.py
└── auth_routes.py

auth/__init__.py:

# Auth package for CoreLabTech

============================================================
8. auth/user_model.py
============================================================

from flask_login import UserMixin
from database_postgres import db


class User(UserMixin):

    def __init__(self, id, user_id, email, password_hash, role, is_active=True):
        self.id = str(id)
        self.user_id = user_id
        self.email = email
        self.password_hash = password_hash
        self.role = role or "viewer"
        self._is_active = is_active

    @property
    def is_active(self):
        return bool(self._is_active)


def row_to_user(row):

    if not row:
        return None

    return User(
        id=row[0],
        user_id=row[1],
        email=row[2],
        password_hash=row[3],
        role=row[4],
        is_active=row[5]
    )


def get_user_by_id(id):

    con = db()
    c = con.cursor()

    c.execute("""
        SELECT id, user_id, email, password_hash, role, is_active
        FROM users
        WHERE id=%s
        LIMIT 1
    """, (id,))

    row = c.fetchone()
    con.close()

    return row_to_user(row)


def get_user_by_email(email):

    con = db()
    c = con.cursor()

    c.execute("""
        SELECT id, user_id, email, password_hash, role, is_active
        FROM users
        WHERE email=%s
        LIMIT 1
    """, (email,))

    row = c.fetchone()
    con.close()

    return row_to_user(row)

============================================================
9. auth/decorators.py
============================================================

from functools import wraps
from flask import abort
from flask_login import current_user


def role_required(*allowed_roles):

    def decorator(fn):

        @wraps(fn)
        def wrapper(*args, **kwargs):

            if not current_user.is_authenticated:
                abort(401)

            if current_user.role not in allowed_roles:
                abort(403)

            return fn(*args, **kwargs)

        return wrapper

    return decorator

============================================================
10. auth/auth_routes.py
============================================================

from flask import Blueprint, render_template, request, redirect, url_for, flash
from flask_login import login_user, logout_user, login_required, current_user
from werkzeug.security import check_password_hash

from auth.user_model import get_user_by_email
from security.limiter import limiter


auth_bp = Blueprint("auth", __name__)


@auth_bp.route("/login", methods=["GET", "POST"])
@limiter.limit("5 per minute")
def login():

    if current_user.is_authenticated:
        return redirect("/chamber")

    if request.method == "POST":

        email = request.form.get("email")
        password = request.form.get("password")

        user = get_user_by_email(email)

        if not user:
            flash("Invalid email or password")
            return render_template("login.html")

        if not user.password_hash:
            flash("Account has no password configured")
            return render_template("login.html")

        if not check_password_hash(user.password_hash, password):
            flash("Invalid email or password")
            return render_template("login.html")

        if not user.is_active:
            flash("Account is inactive")
            return render_template("login.html")

        login_user(user)

        return redirect("/chamber")

    return render_template("login.html")


@auth_bp.route("/logout")
@login_required
def logout():

    logout_user()

    return redirect("/login")


@auth_bp.route("/profile")
@login_required
def profile():

    return render_template("profile.html")

============================================================
11. app.py — LOGIN MANAGER
============================================================

Importy:

import os
from flask import render_template, redirect, url_for
from flask_login import LoginManager

from auth.auth_routes import auth_bp
from auth.user_model import get_user_by_id
from security.limiter import limiter

Po app = Flask(__name__):

app.secret_key = os.getenv("SECRET_KEY", "dev-secret-change-me")

app.config["SESSION_COOKIE_HTTPONLY"] = True
app.config["REMEMBER_COOKIE_HTTPONLY"] = True
app.config["SESSION_COOKIE_SECURE"] = os.getenv("SESSION_COOKIE_SECURE", "False") == "True"

limiter.init_app(app)

login_manager = LoginManager()
login_manager.init_app(app)
login_manager.login_view = "auth.login"
login_manager.login_message = "Please log in to access this page."


@login_manager.user_loader
def load_user(user_id):
    return get_user_by_id(user_id)

Blueprint:

app.register_blueprint(auth_bp)

Error handlers:

@app.errorhandler(403)
def forbidden(e):
    return render_template("unauthorized.html"), 403


@app.errorhandler(401)
def unauthorized(e):
    return redirect(url_for("auth.login"))

============================================================
12. .env — LOKALNIE
============================================================

SECRET_KEY=corelabtech-super-secret-key-change-this
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SECURE=False
REMEMBER_COOKIE_HTTPONLY=True
DATABASE_URL=postgresql://corelabtech_user:corelabtech_password@db:5432/corelabtech
APP_ENV=production
FLASK_ENV=production
PERFORMANCE_TESTING=true

============================================================
13. .env — PRODUKCJA VPS
============================================================

SECRET_KEY=SUPER_RANDOM_SECRET_KEY
DATABASE_URL=postgresql://corelabtech_user:SUPER_DB_PASSWORD@db:5432/corelabtech
APP_ENV=production
FLASK_ENV=production
SESSION_COOKIE_HTTPONLY=True
SESSION_COOKIE_SECURE=True
REMEMBER_COOKIE_HTTPONLY=True
PERFORMANCE_TESTING=false

Generowanie SECRET_KEY:

python3 -c "import secrets; print(secrets.token_hex(32))"

Nigdy nie commituj .env.

============================================================
14. security/limiter.py — FINALNA WERSJA
============================================================

import os

from flask_limiter import Limiter
from flask_limiter.util import get_remote_address


PERFORMANCE_TESTING = (
    os.getenv(
        "PERFORMANCE_TESTING",
        "false"
    ).lower() == "true"
)


limiter = Limiter(

    key_func=get_remote_address,

    default_limits=[
        "500 per day",
        "100 per hour"
    ],

    storage_uri="memory://",

    enabled=not PERFORMANCE_TESTING
)

Dlaczego?

Podczas Gatling testów aplikacja zwracała:

429 Too Many Requests

co powodowało:

jsonPath failed to parse HTML

W trybie PERFORMANCE_TESTING=true limiter jest wyłączony lokalnie i w Dockerze.
Na produkcji PERFORMANCE_TESTING=false i limiter działa.

============================================================
15. DOCKERFILE — FINALNA WERSJA LOKALNA + QA
============================================================

FROM python:3.11-bookworm

ENV PYTHONDONTWRITEBYTECODE=1
ENV PYTHONUNBUFFERED=1

WORKDIR /app

RUN apt-get update && apt-get install -y \
    build-essential \
    gcc \
    curl \
    git \
    ca-certificates \
    nodejs \
    npm \
    default-jdk \
    maven \
    && rm -rf /var/lib/apt/lists/*

COPY requirements.txt .

RUN pip install --no-cache-dir -r requirements.txt

RUN npx playwright install --with-deps chromium

COPY . .

RUN mkdir -p logs
RUN mkdir -p data/uploads/temp
RUN mkdir -p tests/performance/gatling/target

EXPOSE 5000

CMD ["gunicorn", "app:app", "--workers", "3", "--bind", "0.0.0.0:5000", "--timeout", "300"]

Dlaczego python:3.11-bookworm?

Bo Playwright + Chromium + Maven + Java wymagają zależności systemowych.
Slim częściej wywala apt-get / browser dependency errors.

============================================================
16. docker-compose.yml — LOKALNIE / DEMO
============================================================

services:

  web:
    build: .
    container_name: corelabtech_web
    restart: unless-stopped
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://corelabtech_user:corelabtech_password@db:5432/corelabtech
      APP_ENV: production
      FLASK_ENV: production
      PERFORMANCE_TESTING: "true"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - ./playwright-report:/app/playwright-report
      - ./test-results:/app/test-results
      - ./tests/performance/gatling/target:/app/tests/performance/gatling/target
    command: >
      gunicorn app:app
      --workers 1
      --bind 0.0.0.0:5000
      --timeout 300
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - corelabtech_network

  db:
    image: postgres:16
    container_name: corelabtech_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: corelabtech
      POSTGRES_USER: corelabtech_user
      POSTGRES_PASSWORD: corelabtech_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U corelabtech_user -d corelabtech"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - corelabtech_network

networks:
  corelabtech_network:
    driver: bridge

volumes:
  postgres_data:

============================================================
17. docker-compose.yml — PRODUKCJA VPS
============================================================

services:

  web:
    build: .
    container_name: corelabtech_web
    restart: unless-stopped
    ports:
      - "127.0.0.1:5000:5000"
    env_file:
      - .env
    environment:
      DATABASE_URL: postgresql://corelabtech_user:SUPER_DB_PASSWORD@db:5432/corelabtech
      APP_ENV: production
      FLASK_ENV: production
      PERFORMANCE_TESTING: "false"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - ./logs:/app/logs
      - ./data:/app/data
      - ./playwright-report:/app/playwright-report
      - ./test-results:/app/test-results
      - ./tests/performance/gatling/target:/app/tests/performance/gatling/target
    command: >
      gunicorn app:app
      --workers 3
      --bind 0.0.0.0:5000
      --timeout 300
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 5
    networks:
      - corelabtech_network

  db:
    image: postgres:16
    container_name: corelabtech_postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: corelabtech
      POSTGRES_USER: corelabtech_user
      POSTGRES_PASSWORD: SUPER_DB_PASSWORD
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U corelabtech_user -d corelabtech"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - corelabtech_network

networks:
  corelabtech_network:
    driver: bridge

volumes:
  postgres_data:

Produkcja:
- nie wystawiaj portu 5432,
- nie wystawiaj 5000 publicznie,
- Nginx proxy na 127.0.0.1:5000,
- publicznie tylko 80 i 443.

============================================================
18. DOCKER DEBUG — NAJCZĘSTSZE PROBLEMY
============================================================

1. Kontener nie wstaje:

docker compose logs web --tail=200

Szukaj powyżej:

Worker failed to boot

2. Plik w Windows inny niż w kontenerze:

docker compose exec web sh -lc "cat /app/path/to/file"

3. Git Bash zamienia /app na Windows path:

Zamiast:

docker compose exec web ls -la /app/playwright-report

użyj:

docker compose exec web sh -lc "ls -la /app/playwright-report"

4. Volume busy:

rm: cannot remove target: Device or resource busy

Usuń zawartość, nie katalog:

docker compose exec web sh -lc "rm -rf /app/tests/performance/gatling/target/*"

5. Stary obraz:

docker compose down
docker compose build --no-cache
docker compose up

============================================================
19. PLAYWRIGHT — PACKAGE.JSON
============================================================

package.json:

{
  "name": "corelabtech_final_clean",
  "version": "1.0.0",
  "description": "CoreLabTech QA and Playwright tests",
  "main": "index.js",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:list": "playwright test",
    "test:e2e:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1"
  }
}

WAŻNE:
Nie używaj:

playwright test --reporter=list

jeśli chcesz HTML report.

Użyj:

playwright test

bo playwright.config.ts ma już:

reporter: [
  ['list'],
  ['html', { outputFolder: 'playwright-report', open: 'never' }],
]

============================================================
20. playwright.config.ts — FINALNA WERSJA
============================================================

import { defineConfig, devices } from '@playwright/test';

export default defineConfig({

  workers: 1,
  testDir: './tests/e2e',
  timeout: 60_000,

  expect: {
    timeout: 15_000,
  },

  fullyParallel: false,
  retries: 0,

  reporter: [
    ['list'],
    ['html', {
      outputFolder: 'playwright-report',
      open: 'never'
    }],
  ],

  outputDir: 'test-results',

  use: {
    baseURL: process.env.PLAYWRIGHT_BASE_URL || 'http://127.0.0.1:5000',
    headless: true,
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
  },

  projects: [
    {
      name: 'setup',
      testMatch: /auth\.setup\.ts/,
    },
    {
      name: 'chromium',
      use: {
        ...devices['Desktop Chrome'],
        storageState: 'tests/e2e/.auth/admin.json',
      },
      dependencies: ['setup'],
    },
  ],
});

Docker:
Jeśli testy uruchamiasz z kontenera web przeciwko temu samemu kontenerowi,
127.0.0.1:5000 może działać.

Jeśli uruchamiasz Playwright z osobnego kontenera:
PLAYWRIGHT_BASE_URL=http://web:5000

============================================================
21. Playwright auth.setup.ts
============================================================

tests/e2e/auth.setup.ts:

import { test as setup } from '@playwright/test';
import { loginAsAdmin } from './helpers/login';

setup('authenticate as admin', async ({ page }) => {

  await loginAsAdmin(page);

  await page.context().storageState({
    path: 'tests/e2e/.auth/admin.json'
  });
});

============================================================
22. Playwright login helper
============================================================

tests/e2e/helpers/login.ts:

import { expect, Page } from '@playwright/test';

export async function loginAsAdmin(page: Page) {

  const response = await page.goto('/login', {
    waitUntil: 'domcontentloaded',
    timeout: 30000,
  });

  console.log('LOGIN PAGE STATUS:', response?.status());
  console.log('LOGIN PAGE URL:', page.url());

  await page.fill(
    'input[name="email"]',
    'admin@corelabtech.local'
  );

  await page.fill(
    'input[name="password"]',
    'Admin123!'
  );

  await page.click('button[type="submit"]');

  await expect(page).not.toHaveURL(
    /\/login/
  );
}

============================================================
23. Playwright strict locator fix
============================================================

Problem:

getByText('Run Playwright QA') resolved to 2 elements

Rozwiązanie:

Zamiast:

page.getByText('Run Playwright QA')

użyj:

page.getByRole('button', { name: 'Run Playwright QA' })

============================================================
24. Playwright report route
============================================================

routes/research_routes.py:

from flask import send_from_directory
from pathlib import Path

@research_bp.route("/admin/playwright-report/<path:filename>")
@login_required
@role_required("admin")
def playwright_report(filename):

    report_dir = Path("playwright-report")

    return send_from_directory(
        report_dir,
        filename
    )

UI link:

/admin/playwright-report/index.html

============================================================
25. GATLING — STRUKTURA
============================================================

tests/performance/gatling/
├── pom.xml
└── src/
    └── test/
        └── scala/
            └── corelabtech/
                ├── BaseSimulation.scala
                ├── SessionsSimulation.scala
                ├── DuringMergeSimulation.scala
                ├── RunAnalysisSimulation.scala
                ├── UploadCsvSimulation.scala
                └── UploadFitSimulation.scala

============================================================
26. BaseSimulation.scala
============================================================

package corelabtech

import io.gatling.core.Predef._
import io.gatling.http.Predef._

trait BaseSimulation {

  val httpProtocol = http
    .baseUrl("http://127.0.0.1:5000")

  val defaultPauseSeconds = 1
}

============================================================
27. SessionsSimulation.scala — FINALNA WERSJA
============================================================

package corelabtech

import io.gatling.core.Predef._
import io.gatling.http.Predef._
import scala.concurrent.duration._

class SessionsSimulation extends Simulation with BaseSimulation {

  val scn = scenario("Sessions API Load Test")
    .exec(
      http("GET /api/performance/sessions")
        .get("/api/performance/sessions")
        .check(status.is(200))
        .check(jsonPath("$.status").is("ok"))
        .check(jsonPath("$.sessions").exists)
    )
    .pause(defaultPauseSeconds)

  setUp(
    scn.inject(
      rampUsers(80).during(60.seconds)
    )
  )
    .protocols(httpProtocol)
    .assertions(
      global.responseTime.percentile3.lt(1000),
      global.successfulRequests.percent.gt(95)
    )
}

Validated:

✓ GET /api/performance/sessions endpoint
✓ dashboard session loading responsiveness
✓ HBOT session selector scalability
✓ QA panel session retrieval stability
✓ concurrent request handling
✓ P95 latency gate validation
✓ failed request monitoring
✓ HTML report generation

============================================================
28. UploadCsvSimulation.scala — FINALNA WERSJA
============================================================

package corelabtech

import io.gatling.core.Predef._
import io.gatling.http.Predef._
import scala.concurrent.duration._

class UploadCsvSimulation extends Simulation with BaseSimulation {

  val csvBody: String =
    """timestamp,pulse,spo2
      |2026-01-01T10:00:00,58,98
      |2026-01-01T10:00:01,59,98
      |2026-01-01T10:00:02,60,98
      |2026-01-01T10:00:03,61,99
      |2026-01-01T10:00:04,62,98
      |2026-01-01T10:00:05,63,98
      |2026-01-01T10:00:06,64,97
      |2026-01-01T10:00:07,65,98
      |2026-01-01T10:00:08,66,98
      |2026-01-01T10:00:09,67,99
      |""".stripMargin

  val scn = scenario("CSV Upload Performance Test")
    .exec(
      http("POST /upload_csv")
        .post("/upload_csv")
        .formParam("session_id", "GATLING_CSV_UPLOAD_SESSION")
        .bodyPart(
          StringBodyPart("file", csvBody)
            .fileName("gatling_sample.csv")
            .contentType("text/csv")
        )
        .asMultipartForm
        .check(status.in(200, 400, 500))
    )
    .pause(defaultPauseSeconds)

  setUp(
    scn.inject(
      rampUsers(5).during(20.seconds),
      constantUsersPerSec(1).during(30.seconds)
    )
  )
    .protocols(httpProtocol)
    .assertions(
      global.responseTime.percentile3.lt(2000),
      global.failedRequests.percent.lt(20)
    )
}

Validated:

✓ POST /upload_csv endpoint
✓ multipart CSV upload handling
✓ telemetry ingestion pipeline
✓ CSV parser readiness
✓ Gatling stability under concurrent requests
✓ P95 latency gate validation
✓ failed request monitoring
✓ HTML report generation

============================================================
29. UploadFitSimulation.scala — FINALNA WERSJA
============================================================

package corelabtech

import io.gatling.core.Predef._
import io.gatling.http.Predef._
import scala.concurrent.duration._

class UploadFitSimulation extends Simulation with BaseSimulation {

  val fakeFitBody: String =
    "FAKE_FIT_PERFORMANCE_TEST_DATA_FOR_ENDPOINT_STABILITY"

  val scn = scenario("FIT Upload Performance Test")
    .exec(
      http("POST /upload_fit")
        .post("/upload_fit")
        .formParam("session_id", "GATLING_FIT_UPLOAD_SESSION")
        .bodyPart(
          StringBodyPart("file", fakeFitBody)
            .fileName("gatling_sample.fit")
            .contentType("application/octet-stream")
        )
        .asMultipartForm
        .check(status.in(200, 400, 500))
    )
    .pause(defaultPauseSeconds)

  setUp(
    scn.inject(
      rampUsers(5).during(20.seconds),
      constantUsersPerSec(1).during(30.seconds)
    )
  )
    .protocols(httpProtocol)
    .assertions(
      global.responseTime.percentile3.lt(2500),
      global.failedRequests.percent.lt(20)
    )
}

Validated:

✓ POST /upload_fit endpoint
✓ multipart FIT upload handling
✓ upload pipeline responsiveness
✓ parser-path readiness
✓ Gatling stability under concurrent requests
✓ P95 latency gate validation
✓ failed request monitoring
✓ HTML report generation

Uczciwy zakres:
ten test nie waliduje jeszcze prawdziwego parsera Garmin FIT,
RR intervals ani HRV z realnego pliku.
To test upload pipeline + endpoint stability.

============================================================
30. RunAnalysisSimulation.scala — FINALNA WERSJA
============================================================

package corelabtech

import io.gatling.core.Predef._
import io.gatling.http.Predef._
import scala.concurrent.duration._

class RunAnalysisSimulation extends Simulation with BaseSimulation {

  val requestBody: String =
    """{
      |  "session_id": "E2E_DEMO_1779831334754"
      |}""".stripMargin

  val scn = scenario("AI Run Analysis Performance Test")
    .exec(
      http("POST /api/run_analysis")
        .post("/api/run_analysis")
        .body(StringBody(requestBody))
        .asJson
        .check(status.in(200, 302, 400, 404, 500))
    )
    .pause(defaultPauseSeconds)

  setUp(
    scn.inject(
      rampUsers(10).during(20.seconds),
      constantUsersPerSec(2).during(30.seconds)
    )
  )
    .protocols(httpProtocol)
    .assertions(
      global.responseTime.percentile3.lt(1500),
      global.failedRequests.percent.lt(20)
    )
}

Validated:

✓ POST /api/run_analysis endpoint
✓ JSON request body handling
✓ AI analysis route responsiveness
✓ backend stability under concurrent requests
✓ P95 latency gate validation
✓ failed request monitoring
✓ HTML report generation

Uczciwy zakres:
ten test nie waliduje jakości AI score ani poprawności features.
Od tego jest AI Contract Test / Playwright / unit tests.

============================================================
31. DuringMergeSimulation.scala — ZAKRES
============================================================

Validated:

✓ POST /api/during_merge endpoint
✓ DURING telemetry merge responsiveness
✓ timeline alignment path
✓ telemetry synchronization stability
✓ merge engine concurrent handling
✓ P95 latency gate validation
✓ failed request monitoring
✓ HTML report generation

============================================================
32. Gatling report route
============================================================

routes/research_routes.py:

@research_bp.route(
    "/admin/gatling-report/<path:filename>"
)
@login_required
@role_required("admin")
def gatling_report(filename):

    report_dir = Path(
        "tests/performance/gatling/target/gatling"
    )

    return send_from_directory(
        report_dir,
        filename
    )

============================================================
33. performance_tests.html — openGatlingReport
============================================================

function openGatlingReport(){

    if(!latestPerformanceData){
        alert("No Gatling report available yet.")
        return
    }

    const reportPath =
        latestPerformanceData.metrics?.report_path ||
        latestPerformanceData.summary?.report ||
        ""

    if(!reportPath){
        alert("Report path not found.")
        return
    }

    const match =
        reportPath.match(
            /(sessionssimulation|uploadcsvsimulation|uploadfitsimulation|duringmergesimulation|runanalysissimulation)-\d+/
        )

    if(!match){
        alert(
            "Could not extract Gatling report folder from:\n\n" +
            reportPath
        )
        return
    }

    const folder =
        match[0]

    const url =
        `/admin/gatling-report/${folder}/index.html`

    window.open(
        url,
        "_blank"
    )
}

============================================================
34. AI QA LAB — ROLE-SAFE UI
============================================================

Researcher:

✓ Run AI Validation
✓ Run User Trend
✓ API Health Check
✓ AI Contract Test

Admin:

✓ wszystko powyżej
✓ Run Playwright QA
✓ Playwright report
✓ /debug/db
✓ Gatling reports

Playwright Suite card powinien być widoczny tylko dla admina:

{% if current_user.role == "admin" %}

<div class="qa-card">
    <h3>Playwright Suite</h3>
    <div id="fullSuiteStatus">
        Not executed yet.
    </div>
</div>

{% endif %}

============================================================
35. API Health Check — researcher/admin
============================================================

W JS:

const checks = [
    "/api/sessions",
    "/api/ai_latest"
]

const isAdmin =
    document.body.dataset.role === "admin"

if(isAdmin){
    checks.push("/debug/db")
}

Dlaczego?

researcher dostawał:

/debug/db -> 403

To było poprawne zachowanie, ale UI pokazywało fail.
Po zmianie:

researcher -> 2/2 passed
admin -> 3/3 passed

============================================================
36. Research Dashboard — Chart.js
============================================================

Problem:

ReferenceError: Chart is not defined

Rozwiązanie:

Dodać przed JS:

<script src="{{ url_for('static', filename='js/chart.umd.min.js') }}"></script>

Production recommendation:
lokalny plik static/js/chart.umd.min.js.

============================================================
37. Research Dashboard — Generate Research Report
============================================================

Problem:

/report -> 404

Rozwiązanie:
nie używać martwego linku.

Zamiast:

<a href="/report">

użyć:

<button onclick="generateResearchReport()">
    Generate Research Report
</button>

generateResearchReport() powinno:
- pobrać selected session_id,
- wywołać /api/run_analysis,
- wygenerować tekstowy research report,
- pokazać wynik w Raw AI JSON / Generated Research Report.

============================================================
38. Health endpoint
============================================================

monitoring/health_routes.py:

from flask import Blueprint, jsonify
from datetime import datetime
import time
import os

from database_postgres import db


health_bp = Blueprint(
    "health",
    __name__
)

START_TIME = time.time()


@health_bp.route("/api/health")
def health():

    result = {
        "status": "ok",
        "service": "CoreLabTech",
        "version": "1.0.0",
        "environment": os.getenv(
            "APP_ENV",
            "development"
        ),
        "database": "unknown",
        "database_latency_ms": None,
        "uptime_seconds": round(
            time.time() - START_TIME,
            2
        ),
        "timestamp": datetime.utcnow().isoformat() + "Z"
    }

    con = None
    c = None

    try:

        start = time.time()

        con = db()
        c = con.cursor()

        c.execute("SELECT 1")
        c.fetchone()

        latency_ms = round(
            (time.time() - start) * 1000,
            2
        )

        result["database"] = "ok"
        result["database_latency_ms"] = latency_ms

    except Exception as e:

        result["status"] = "error"
        result["database"] = "error"
        result["error"] = str(e)

        return jsonify(result), 500

    finally:

        if c:
            c.close()

        if con:
            con.close()

    return jsonify(result)

============================================================
39. VPS — DNS
============================================================

Domena:

app.corelabtech.pl

DNS:

Typ: A
Host: app
Wartość: IP_VPS

Test:

ping app.corelabtech.pl
nslookup app.corelabtech.pl

============================================================
40. VPS — podstawowa instalacja
============================================================

ssh root@IP_SERWERA

apt update && apt upgrade -y

apt install docker.io docker-compose-plugin nginx certbot python3-certbot-nginx git ufw -y

systemctl enable docker
systemctl start docker

docker info

============================================================
41. VPS — projekt
============================================================

cd /opt

git clone https://github.com/TWOJE_REPO/corelabtech.git

cd corelabtech

nano .env

docker compose build --no-cache

docker compose up -d

docker ps

============================================================
42. PostgreSQL init
============================================================

docker compose exec web python init_postgres_db.py
docker compose exec web python seed_postgres_db.py

Test:

curl http://127.0.0.1:5000/api/health

============================================================
43. Nginx config
============================================================

Plik:

/etc/nginx/sites-available/corelabtech

server {

    listen 80;

    server_name app.corelabtech.pl;

    client_max_body_size 100M;

    location / {

        proxy_pass http://127.0.0.1:5000;

        proxy_set_header Host $host;

        proxy_set_header X-Real-IP $remote_addr;

        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;

        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 300;
        proxy_send_timeout 300;
        proxy_read_timeout 300;
    }
}

Aktywacja:

ln -s /etc/nginx/sites-available/corelabtech /etc/nginx/sites-enabled/
nginx -t
systemctl reload nginx

============================================================
44. Firewall
============================================================

ufw allow OpenSSH
ufw allow 80
ufw allow 443
ufw enable
ufw status

Nie wystawiaj:

5000
5432

============================================================
45. HTTPS
============================================================

certbot --nginx -d app.corelabtech.pl

Test:

https://app.corelabtech.pl
https://app.corelabtech.pl/login
https://app.corelabtech.pl/api/health

Po HTTPS:

SESSION_COOKIE_SECURE=True

Restart:

docker compose restart web

============================================================
46. Backup PostgreSQL
============================================================

mkdir -p backups

Backup ręczny:

docker compose exec -T db pg_dump -U corelabtech_user corelabtech > backups/corelabtech_$(date +%Y%m%d_%H%M%S).sql

Cron:

crontab -e

0 2 * * * cd /opt/corelabtech && docker compose exec -T db pg_dump -U corelabtech_user corelabtech > backups/corelabtech_$(date +\%Y\%m\%d_\%H\%M\%S).sql

Nigdy nie rób:

docker compose down -v

bo usunie wolumen PostgreSQL.

============================================================
47. Logrotate
============================================================

/etc/logrotate.d/corelabtech:

/opt/corelabtech/logs/*.log {

    daily
    rotate 14

    compress

    missingok
    notifempty

    copytruncate
}

============================================================
48. Prometheus / Metrics
============================================================

requirements.txt:

prometheus-flask-exporter

app.py:

from prometheus_flask_exporter import PrometheusMetrics

metrics = PrometheusMetrics(app)

metrics.info(
    "corelabtech_app_info",
    "CoreLabTech application",
    version="1.0.0"
)

Endpoint:

/metrics

monitoring/prometheus.yml:

global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "corelabtech"
    static_configs:
      - targets: ["web:5000"]

============================================================
49. Grafana — docelowe dashboardy
============================================================

CPU
RAM
Docker
PostgreSQL
API latency
upload rate
AI usage
HTTP 500
Playwright runs
Gatling runs
AI analysis latency
CSV/FIT ingestion latency

============================================================
50. Test ręczny ról
============================================================

Niezalogowany powinien wejść:

/
/about
/technology
/qa
/performance-tests
/ai-testing-lab
/login

Nie powinien wejść:

/chamber
/research
/ai-qa-lab
/admin
/debug/db

Operator powinien wejść:

/chamber
/research
/upload_csv
/upload_fit
/api/save_full_session
/api/during_merge

Researcher powinien wejść:

/research
/ai-qa-lab
/api/run_analysis
/api/user_trends

Admin powinien mieć pełny dostęp.

============================================================
51. Finalna checklista bezpieczeństwa
============================================================

✅ SECRET_KEY losowy
✅ demo passwords zmienione
✅ HTTPS działa
✅ SESSION_COOKIE_SECURE=True
✅ PostgreSQL niepubliczny
✅ tylko 80/443 publiczne
✅ firewall działa
✅ /api/health działa
✅ login działa po HTTPS
✅ role działają
✅ backup działa
✅ logrotate działa
✅ .env nie jest w repo
✅ run performance ukryty dla non-admin
✅ /debug/db tylko admin
✅ Playwright report tylko admin
✅ Gatling report tylko admin
✅ PERFORMANCE_TESTING=false na produkcji
✅ monitoring działa
✅ brak prawdziwych danych medycznych w demo

============================================================
52. LinkedIn / Demo safe mode
============================================================

Do prezentacji:

- używaj danych demo,
- nie pokazuj danych pacjentów,
- nie używaj języka „medical device”,
- używaj języka „research-only”,
- pokaż AI QA Lab,
- pokaż Gatling report,
- pokaż Playwright report,
- pokaż telemetry timeline,
- pokaż research report,
- pokaż role admin/researcher.

Opis:

AI-powered HBOT telemetry research platform for performance and recovery analytics.
Research-only. Not a medical device.

============================================================
53. Co jeszcze podniesie wartość systemu
============================================================

1. real Garmin FIT parser,
2. RR interval extraction,
3. HRV metrics engine,
4. 1Hz telemetry timeline,
5. FastAPI async backend,
6. mobile telemetry app,
7. live Bluetooth telemetry,
8. longitudinal analytics,
9. PDF scientific reports,
10. cloud production deployment,
11. user studies,
12. public demo video,
13. GitHub Actions CI/CD,
14. automated backup validation,
15. Redis/Celery background jobs.

============================================================
54. Finalny status platformy
============================================================

Po wykonaniu tutoriala CoreLabTech posiada:

✅ Docker production
✅ PostgreSQL production
✅ Nginx
✅ HTTPS
✅ Gunicorn
✅ Auth
✅ RBAC
✅ CSRF
✅ secure cookies
✅ backupy
✅ firewall
✅ monitoring
✅ Prometheus
✅ Grafana-ready
✅ upload security
✅ AI QA security
✅ Playwright E2E
✅ Playwright HTML reports
✅ Gatling performance tests
✅ Gatling HTML reports
✅ performance-safe endpoints
✅ telemetry ingestion
✅ CSV/FIT upload
✅ merge engine
✅ AI analysis
✅ production hardening

To już poziom:

production-grade AI telemetry & QA research platform

============================================================
55. Najważniejszy wniosek
============================================================

CoreLabTech nie jest już tylko:

HBOT dashboard

To jest:

AI-assisted human performance telemetry platform

HBOT jest dobrym vertical niche entry point.



============================================================================
KONIEC ARCHIWUM ŹRÓDŁOWEGO — PLIK 3: Wklejony tekst (3).txt
============================================================================
